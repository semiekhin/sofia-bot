# База знаний Sofia-GPT

## Ключевые решения

### 13 января 2026: Диагностика повторов

### 20.01.2026: Финансовый модуль v1.1 — безопасная интеграция

**Проблема:** Предыдущая интеграция финансового модуля (v1.0) сломала логику квалификации — меняли Planner, State, Extractor одновременно.

**Решение:** Минимальная интеграция БЕЗ изменения Planner:
1. extractor.py — добавлен question_type "profitability"
2. bot_server.py — обогащение контекста расчётом (~10 строк)
3. sofia_prompt.py — вывод finance_calculation + пример

**Ключевой принцип:** 
- Planner выбирает ANSWER_QUESTION (как обычно)
- bot_server добавляет расчёт в контекст если бюджет известен
- Generator использует цифры

**Почему безопасно:** Логика квалификации не затронута. Финансы — просто обогащение ответа.

---

### 20.01.2026: Трансляция диалогов userbot

**Задача:** Наблюдать за диалогами userbot в реальном времени.

**Решение:**
- Группа "Диалоги Софья" (ID: -5206139579)
- Функция `broadcast_to_observers()` в userbot
- Вызов после каждого ответа в handle_message

**Формат сообщения:**
```
💬 Диалог с {user_name} (ID: {user_id})
👤 Клиент: {сообщение}
🤖 София: {ответ}
```

---


### 20.01.2026: Унификация промпта и "Живая София"

**Проблема:** bot_server.py генерировал более "зажатые" ответы чем userbot.

**Причина:** bot_server не передавал `action_context` в `get_system_prompt()`:
```python
# bot_server.py (было)
base_prompt = get_system_prompt(user_name)  # БЕЗ action_context
# + свой урезанный action_directive
# + stage_context (дублирование)

# userbot (было правильно)
system_prompt = get_system_prompt(user_name, action_context)
```

**Решение:** 
1. Унифицировали bot_server.py по образцу userbot
2. Добавили `situation` и `optional_hints` в action_context (planner.py)
3. Добавили раздел "ТВОЯ ЛИЧНОСТЬ" в промпт (sofia_prompt.py)
4. Расширили паттерны extractor для "сдавать" → strategy: rental

**Ключевой принцип:** Userbot импортирует модули, поэтому изменения в planner.py и sofia_prompt.py автоматически применяются к обоим.

---

### 20.01.2026: Архитектурное решение — Planner vs RAG

**Вопрос:** Можно ли уйти от детерминированного Planner и выбирать действия через RAG?

**Ответ:** НЕТ, это создаст проблемы:
- RAG не знает state (может переспросить уже известное)
- RAG не умеет считать (slot_attempts, call_proposal_count)
- Семантическое сходство ≠ логическое соответствие

**Правильная архитектура:**
- Planner (КОД) = ЧТО делать (100% предсказуемость)
- RAG + Generator (LLM) = КАК делать (креативность)
- situation + hints = контекст для адаптации стиля

---

**Проблема:** Бот повторяет вопрос о локации 4+ раза
**Причина:** Planner не считает попытки, Extractor не понимает "не знаю"
**Решение:** Нужен счётчик ask_counts в state + лимит 2 попытки
**Статус:** Не реализовано

### 12 января 2026: WAIT-логика только через Planner
**Проблема:** WAIT срабатывал на "35" (бюджет) потому что "записала" в тексте бота
**Решение:** Закомментированы строки 130-131 в planner.py
**Почему:** Planner проверяет реальный флаг state.meeting_agreed, а не угадывает по тексту

### 10 января 2026: Агентная архитектура
**Решение:** Extractor → State Manager → Planner → Generator
**Результат:** GOOD rate вырос с 64.8% до 93.75%
**Почему:** LLM не умеют считать и следовать жёстким правилам — это делает код

## Известные проблемы и решения

### Проблема: Повтор вопросов (4+ раза)
**Симптомы:** Бот спрашивает "Что по локации?" 4 раза подряд
**Причина:** Нет лимита повторов в Planner
**Решение:** Добавить ask_counts в state_manager.py + проверку в planner.py
**Файлы:** state_manager.py, planner.py

### Проблема: Extractor не понимает "не знаю"
**Симптомы:** location остаётся null после "не знаю"/"где выгоднее"
**Причина:** В промпте Extractor нет правила для неопределённых ответов
**Решение:** Добавить location: "any" для "не знаю" (как payment_type)
**Файл:** extractor.py, строки 70-130

### Проблема: "Вы на связи?" спам
**Симптомы:** Бот повторяет это после каждого сообщения
**Причина:** Не диагностирована
**Файл:** Искать в bot_server.py или sofia_prompt.py

### Проблема: assistant сохраняется с user_id=0
**Симптомы:** SELECT WHERE role='assistant' AND user_id=X возвращает 0
**Причина:** Баг в save_message()
**Файл:** bot_server.py

## Паттерны и практики

### Детерминированная логика > промпт
LLM плохо считают и следуют правилам. Выносить в код:
- Счётчики (количество вопросов, попыток)
- Порядок действий (квалификация → созвон)
- Лимиты (макс 2 вопроса на тему)

### Confidence levels
- `confirmed` — клиент явно сказал "хочу в Сочи"
- `mentioned` — клиент упомянул "слышал про Сочи"
- `null` — не говорил об этом

### Тестирование через feedback_v2
```sql
SELECT rating, COUNT(*) FROM feedback_v2 
WHERE timestamp >= '2026-01-13' 
GROUP BY rating;
```

## Полезные команды
```bash
# Диалог конкретного клиента
sqlite3 /opt/sofia-gpt/sofia_gpt.db "SELECT role, content, timestamp FROM messages WHERE user_id = ID ORDER BY timestamp;"

# Состояние клиента
sqlite3 /opt/sofia-gpt/sofia_gpt.db "SELECT * FROM client_state WHERE user_id = ID;"

# Последние оценки с комментариями
sqlite3 /opt/sofia-gpt/sofia_gpt.db "SELECT rating, comment, timestamp FROM feedback_v2 WHERE comment != '' ORDER BY timestamp DESC LIMIT 10;"

# Статистика по дням
sqlite3 /opt/sofia-gpt/sofia_gpt.db "SELECT DATE(timestamp), rating, COUNT(*) FROM feedback_v2 GROUP BY DATE(timestamp), rating;"

# Поиск в коде
grep -rn "на связи" /opt/sofia-gpt/*.py
```

## Чего НЕ делать

- **Не проверять WAIT по тексту** — только через state.meeting_agreed
- **Не добавлять общие слова в MEETING_MARKERS** — "записала" вызывает ложные срабатывания
- **Не использовать gpt-5.2 с temperature** — не поддерживается
- **Не хранить методы класса в dict** — перезапишутся через setattr
- **Не полагаться на LLM для счёта** — выносить в код

### 15.01.2026: Унификация логики HELP_* для всех слотов

**Проблема:** Payment обрабатывался иначе чем location — "не знаю" превращалось в "any, confirmed", из-за чего HELP_PAYMENT не срабатывал и бот переспрашивал тот же вопрос.

**Решение:** 
1. Унифицировали Extractor: "не знаю"/"надо подумать" → null для ВСЕХ слотов
2. Добавили HELP_LPR (не было)
3. Переписали все HELP_* — теперь объясняют ЗАЧЕМ нужна информация, а не просто переформулируют вопрос

**Почему:** 
- Клиент не идиот — повторение вопроса другими словами раздражает
- Объяснение ценности вопроса мотивирует ответить
- Если после объяснения всё равно "не знаю" — пропускаем слот, не настаиваем

### 17.01.2026 (вечер): Обнаружен баг slot_attempts

**Проблема:** Бот спрашивает один и тот же вопрос (payment_type) 8+ раз подряд, игнорируя лимит попыток.

**Симптомы:**
- Planner выдаёт `ask_payment` бесконечно
- `slot_attempts` в state показывает нули: `{'ask': 0, 'help': 0}`
- Клиент раздражается: "я ж сказал уже", "издеваетесь?"

**Где искать причину:**
1. `planner.py:230-260` — проверка `state.slot_attempts`
2. `bot_server.py:504-509` — вызов `increment_slot_attempt()`
3. `state_manager.py` — сохранение `slot_attempts` в БД (JSON поле)

**Гипотезы:**
- `increment_slot_attempt()` вызывается, но state не сохраняется в БД
- `slot_attempts` загружается из БД как пустой dict
- Логика в Planner проверяет `slot_attempts` до инкремента

**Как диагностировать:**
```bash
# Добавить логи в bot_server.py после инкремента:
print(f"DEBUG slot_attempts after increment: {client_state.slot_attempts}")

# Проверить что в БД:
sqlite3 /opt/sofia-gpt/sofia_gpt.db "SELECT slot_attempts FROM client_state WHERE user_id = ID;"
```

### 17.01.2026: Userbot v2.0 — правильная архитектура

**Решение:** Userbot должен ИМПОРТИРОВАТЬ логику из bot_server.py, а не копировать код.

**Почему:**
- Один источник правды — изменения в боте автоматически работают в userbot
- Общая БД — история диалогов единая
- Меньше багов — не нужно синхронизировать два кодовых потока

**Реализация:**
```python
from bot_server import (
    generate_response_with_rag,
    state_manager,
    extract_sync,
    ...
)
```

**Ограничение:** Userbot в базовой версии использует `/send` для отправки произвольного текста. Умный `/start` (через Planner) был откачен из-за бага slot_attempts.

---

### 17.01.2026 (ночь): Баг slot_attempts — причина и решение
**Проблема:** Бот спрашивал payment_type 8+ раз подряд, slot_attempts не инкрементировался
**Причина:** В bot_server.py action `ask_payment` преобразовывался в slot `"payment"`, а в БД ключ `"payment_type"`
**Решение:** Добавлен маппинг `ACTION_TO_SLOT = {"payment": "payment_type"}` перед вызовом increment_slot_attempt()
**Коммит:** 4cac35fd

### 17.01.2026 (ночь): Спецификация Sofia-GPT v2.0
**Проблема:** Sofia задаёт слишком много вопросов, не вовремя предлагает созвон, игнорирует отказы
**Решение:** Новая архитектура с двумя ветками (INVESTMENT/PERSONAL), двумя предложениями созвона, счётчиком отказов
**Ключевые принципы:**
- Обязательные поля: goal, location (море/горы достаточно), budget
- Необязательные: strategy, usage, family, payment_type, lpr
- При 2 отказах — завершаем, не мучаем клиента
- Формулировки созвона из RAG (скрипты Оксаны)
**Статус:** Спецификация готова, реализация — следующая сессия

### 17.01.2026 (ночь): Логика no_call
**Проблема:** Клиент говорит "не хочу созвон, давайте в ватсап" — бот не понимает
**Решение:** 
- Extractor: добавлен `objection: "no_call"`
- StateManager: добавлен флаг `call_refused`
- Planner: при повторном no_call и call_refused=True — пропускаем HANDLE_OBJECTION
**Почему:** Первый раз отрабатываем возражение (пытаемся убедить), второй раз — сдаёмся

### 17.01.2026 (поздняя ночь): Реализация Sofia-GPT v2.0

**Проблема:** Sofia задавала слишком много вопросов, не вовремя предлагала созвон, игнорировала отказы клиента.

**Решение:** Полная реализация спецификации v2.0:
- Две ветки квалификации (INVESTMENT / PERSONAL) с разным порядком вопросов
- Два предложения созвона (после базовой и полной квалификации)
- Счётчик отказов (при 2 отказах → завершаем с подборкой)
- Формулировки Оксаны в RAG

**Ключевые файлы:**
- `planner.py` — _qualify_investment(), _qualify_personal(), новые Actions
- `state_manager.py` — branch, strategy, usage, family, call_proposal_count, materials_request_count, dialog_finished
- `sofia_prompt.py` — усиленные директивы для PROPOSE_MEETING_*

**Почему:**
- Разный порядок вопросов для инвестора и "для себя" — разные приоритеты
- Два созвона дают второй шанс, но не раздражают бесконечными предложениями
- Счётчик отказов уважает решение клиента — при 2 отказах не настаиваем

### 17.01.2026: Generator игнорирует директивы

**Проблема:** Planner выдаёт PROPOSE_MEETING_1, но Generator генерирует вопрос про локацию вместо предложения созвона.

**Решение:** Усилены директивы в sofia_prompt.py:
```
⚠️ КРИТИЧНО: Если ACTION = PROPOSE_MEETING_1 или PROPOSE_MEETING_2 — ты ОБЯЗАНА предложить созвон!
НЕ задавай других вопросов (локация, оплата и т.д.), ТОЛЬКО предложи созвон.
```

**Почему:** LLM стохастичны и могут игнорировать инструкции. Явные "ОБЯЗАНА" и "КРИТИЧНО" повышают compliance.

---

### 18.01.2026: RAG должен использовать action Planner'а, а не Stage Detector

**Проблема:** RAG искал примеры по `current_stage` от Stage Detector, который угадывал этап по тексту и часто ошибался. Например, `propose_meeting_1` (квалификация закончена → предложить созвон) получал примеры PRESENTATION вместо MEETING.

**Решение:** 
1. Добавлен маппинг `ACTION_TO_RAG_STAGE` в bot_server.py (строка 71)
2. RAG теперь берёт stage по action от Planner: `rag_stage = ACTION_TO_RAG_STAGE.get(action, current_stage)`

**Почему:** Planner точно знает что делает (ASK_STRATEGY = квалификация), а Stage Detector угадывает. RAG должен давать релевантные примеры для текущего ДЕЙСТВИЯ, а не предполагаемого этапа.

**Код:**
```python
ACTION_TO_RAG_STAGE = {
    "ask_goal": "QUALIFICATION",
    "ask_strategy": "QUALIFICATION",
    ...
    "propose_meeting_1": "MEETING",
    "propose_meeting_2": "MEETING",
    "handle_objection": "OBJECTION",
    ...
}
```

---

### 18.01.2026: Баланс "живого общения" и возврата к делу

**Проблема:** Хотели сделать Софию более живой, но боялись что она уйдёт в бесконечный чат.

**Решение:** Формула для off-topic: `ОТВЕТ + мягкий переход к квалификации/созвону`

**Примеры:**
- К: "Как погода в Сочи?" → С: "Сейчас +12, солнышко ☀️ Кстати, вы Сочи как вариант рассматриваете?"
- К: "Кто выиграл ЛЧ?" → С: "Реал, как обычно 😄 Так, а мы на чём остановились — бюджет обсудили?"

**Правила:**
- 1-2 ответа на off-topic + мягкий возврат к теме
- Если клиент настойчиво болтает 2-3 раза — ок, поболтай, он тоже человек


---

### 18.01.2026 (день): Userbot v2.0 — полное восстановление

**Проблема:** Userbot был откачен до версии 1.0 вместе с фиксом slot_attempts (17.01). Потерял: merge_extraction_to_state, increment_slot_attempt, get_system_prompt, ACTION_TO_SLOT.

**Решение:** 
1. Восстановлен из бэкапа `sofia_userbot_pyrogram.py.backup_20260117_132806`
2. Добавлены константы из bot_server.py (не импорт, чтобы не трогать Софью):
   - `ACTION_TO_SLOT = {"payment": "payment_type"}`
   - `ACTION_TO_RAG_STAGE = {...}` (23 маппинга)
   - `RAG_EXAMPLES_COUNT = 10`
   - `MODEL_MODE = os.getenv("MODEL_MODE", "gpt-5.2")`
3. Переключен на gpt-5.2 Responses API
4. Добавлено сохранение v2.0 полей после Planner
5. Добавлено логирование в файл

**Почему константы дублируются, а не импортируются:** Константы определены внутри bot_server.py и не экспортируются. Чтобы импортировать — нужен рефакторинг bot_server.py. Решили не трогать работающий бот.

---

### 18.01.2026 (день): Связанное извлечение goal + strategy

**Проблема:** Клиент говорит "буду сдавать в аренду", Extractor извлекает только goal: investment, но не strategy: rental. Planner переспрашивает про стратегию — это раздражает.

**Решение:** Добавлено правило в extractor.py:
```
СВЯЗАННОЕ ИЗВЛЕЧЕНИЕ (goal + strategy одновременно):
- "сдавать в аренду", "буду сдавать" → goal: investment + strategy: rental
- "для перепродажи", "купить и продать" → goal: investment + strategy: growth
```

**Почему:** Если клиент явно говорит про аренду — это уже ответ на оба вопроса. Не нужно переспрашивать.

---

### 18.01.2026 (день): Архитектура Userbot — импорт vs дублирование

**Принцип:** Userbot ИМПОРТИРУЕТ логику из модулей, но ДУБЛИРУЕТ константы из bot_server.py.

**Что импортируется (общий код):**
- `extractor.py` → extract_sync(), merge_extraction_to_state()
- `state_manager.py` → StateManager, ClientState
- `planner.py` → get_next_action(), increment_slot_attempt()
- `sofia_prompt.py` → get_system_prompt()
- `rag_module.py` → search_examples()

**Что дублируется (константы):**
- ACTION_TO_SLOT
- ACTION_TO_RAG_STAGE
- RAG_EXAMPLES_COUNT
- MODEL_MODE

**Результат:** Изменения в модулях (extractor, planner, etc.) автоматически работают в обоих (бот + userbot). Константы нужно синхронизировать вручную.

---

### 18.01.2026 (вечер): Планируется — Финансовый консультант

**Задача:** Sofia должна уметь:
- Отвечать на финансовые вопросы (ставка ЦБ, доходность депозитов)
- Сравнивать депозит vs недвижимость с расчётами
- Убеждать клиента в выгоде недвижимости
- Продолжать консультировать после FINISH_WITH_MATERIALS

**Архитектура (планируется):**
1. `finance_fetcher.py` — парсинг данных (ЦБ, банки)
2. `finance_calculator.py` — расчёты доходности
3. `finance_data.json` — кэш актуальных данных
4. Новые Actions: CONSULT_FINANCE, COMPARE_DEPOSIT, CALCULATE_YIELD
5. RAG по финансовым диалогам

**Срок:** 1-2 недели после стабилизации текущей версии

---

### 22.01.2026 (ночь): LLM-Planner v3.0

**Задача:** Сделать Sofia умнее в выборе действий — не жёстко по правилам, а с учётом контекста.

**Решение:** Архитектура "allowed_actions + LLM выбор":
1. Детерминированный Planner формирует список допустимых действий (1-5 штук)
2. Если вариантов > 1 → LLM (gpt-5.2, reasoning:high) выбирает лучший
3. LLM возвращает: action + micro_goal + tone
4. micro_goal передаётся Generator для "комбо-ответов"

**Ключевые файлы:**
- `planner.py` — добавлен `get_allowed_actions()`, `_get_next_qualification_action()`, `_get_qualification_after_meeting()`
- `llm_planner.py` — новый модуль, `llm_select_action()`
- `bot_server.py` — интеграция с флагом `USE_LLM_PLANNER`
- `sofia_userbot_pyrogram.py` — синхронизация

**Примеры работы:**
```
Клиент: "Дорого. А рассрочка есть?"
allowed: [handle_objection, answer_question, ask_payment]
LLM выбирает: answer_question + micro_goal: "уточнить параметры рассрочки"
→ Sofia отвечает на вопрос, не "отрабатывает возражение"
```
```
Клиент: "ну ок" (холодный)
allowed: [propose_meeting_1, ask_payment]
signals: engagement=low, call_readiness=0.3
LLM выбирает: ask_payment
→ Sofia не давит созвоном, продолжает квалификацию
```

**Почему это лучше:**
- Детерминированный Planner гарантирует что действие допустимо
- LLM добавляет "человечность" — выбирает что уместнее в контексте
- Fallback на детерминированный Planner при ошибке LLM
- micro_goal позволяет делать "комбо" (ответить + спросить)

**Стоимость:** ~$0.01-0.03 за вызов LLM-Planner (только когда >1 варианта)

**Конфигурация:**
- `USE_LLM_PLANNER=true` в .env
- Модель: gpt-5.2, reasoning.effort: high

---

### 22.01.2026: RAG → LLM-Planner интеграция

**Задача:** Показывать LLM-Planner успешные примеры из RAG при выборе action.

**Решение:**
1. Функция `_get_rag_examples()` в llm_planner.py
2. Триггеры: `objection: "no_call"`, `wants_materials: true`, HANDLE_OBJECTION/PROPOSE_MEETING в allowed_actions
3. RAG примеры добавляются в промпт перед "Выбери лучшее действие"

**Почему:** LLM видит как успешно решали похожие ситуации (аналогия с врачом, "работа уже сделана") и делает более умный выбор.

---

### 22.01.2026: Баг call_proposal_count при USE_LLM_PLANNER

**Проблема:** После первого отказа от созвона бот снова предлагал созвон вместо продолжения квалификации.

**Причина:** `call_proposal_count` инкрементировался только в `_qualify_investment()` / `_qualify_personal()`, но при USE_LLM_PLANNER=true вызывается `get_allowed_actions()`, который только читает счётчик.

**Решение:** Добавлен инкремент в bot_server.py после выбора action:
```python
if action in [Action.PROPOSE_MEETING_1, Action.PROPOSE_MEETING_2]:
    client_state.call_proposal_count += 1
```

**Урок:** При USE_LLM_PLANNER=true side-effects (инкременты счётчиков) должны быть в bot_server.py, не в planner.py.

---

### 22.01.2026: Защита userbot от бесконечных рестартов

**Проблема:** Сессия Telegram истекла → userbot падал → systemd рестартовал (280 раз!) → лимит Telegram.

**Решение:**
1. systemd: `StartLimitBurst=5`, `StartLimitIntervalSec=600` — макс 5 рестартов за 10 мин
2. Код: обработка AUTH_KEY_UNREGISTERED → `sys.exit(0)` без рестарта + инструкция в логах

**Почему:** Бесконечные попытки авторизации блокируют аккаунт Telegram на 1-24 часа.

---

### 28.01.2026: Интеграция Max через Radist.Online

**Задача:** Добавить второй канал связи — Max мессенджер (писать первым).

**Проблема:** Max боты (как и Telegram боты) не могут инициировать диалог.

**Решение:** Radist.Online — шлюз для номерных аккаунтов:
- Подключен номер +79284466701
- API: https://api-ru.radist.online/v2
- company_id: 205054, connection_id: 80024

**Важные открытия:**
1. ВСЕ эндпоинты требуют `/companies/{company_id}/` в пути — без него 404
2. `chat_id` — число, не телефон. Сначала создать чат, потом отправлять
3. Формат text — объект `{"text": "сообщение"}`, не строка
4. Для России использовать `api-ru.radist.online`

**API примеры:**
```bash
# Создать чат
POST /companies/205054/messaging/chats/
{"connection_id": 80024, "phone": "+79001234567"}
→ {"chat_id": 38001832, "contact_id": 32498778}

# Отправить сообщение
POST /companies/205054/messaging/messages/
{"connection_id": 80024, "chat_id": 38001832, "message_type": "text", "text": {"text": "Привет!"}}
```

**Файлы:**
- `docs/RADIST_API.md` — полная документация
- `config/radist_config.py` — конфигурация

**TODO:**
- Настроить webhook для входящих
- Написать sofia_radist_gateway.py

**⚠️ Подписка Radist до 31.01.2026!**

### 28.01.2026: Баг merge_extraction_to_state — strategy/usage/family

**Проблема:** Поля strategy, usage, family никогда не сохранялись в state клиента. Extractor их извлекал, но merge_extraction_to_state() их игнорировал.

**Решение:** Добавили эти поля в simple_fields в extractor.py.

**Почему не замечали:** Planner использует branch (который устанавливается от goal), а не strategy напрямую. Поэтому основная логика работала, но детализация терялась.

### 28.01.2026: Архитектурный долг — дублирование логики

**Проблема:** Логика обработки сообщений продублирована в 3 файлах:
- bot_server.py (Telegram бот)
- sofia_userbot_pyrogram.py (Telegram userbot)  
- sofia_radist_gateway.py (Max gateway)

**Следствие:** Баги фиксятся в одном месте, остаются в других. Каждая интеграция требует копирования 200+ строк кода.

**Решение (TODO):** Вынести общую логику в message_processor.py:
```
process_message(user_id, message, history) -> response
```
Транспорты (Telegram, Max, etc.) только получают/отправляют сообщения.

---

## 29.01.2026: Исправление логики созвонов (КОНТРОЛЬНАЯ ТОЧКА)

### Проблема
После первого отказа от созвона бот снова предлагал созвон вместо продолжения квалификации.

### Причины
1. **Двойной инкремент `call_proposal_count`:**
   - В `planner.py`: `state.call_proposal_count = 1/2`
   - В `bot_server.py`: `+1` после PROPOSE_MEETING
   - После MEETING_1 сразу записывалось 2

2. **Неправильное условие HANDLE_OBJECTION:**
   - `objection = "busy"` считался возражением
   - Generator внутри HANDLE_OBJECTION снова предлагал созвон

### Решение
1. Убрали присвоение `call_proposal_count` из planner.py
2. Изменили условие:
```python
# БЫЛО:
if obj and obj != "no_call":
    return Action.HANDLE_OBJECTION

# СТАЛО:
if obj and obj not in ["no_call", "busy"] and not wants_materials:
    return Action.HANDLE_OBJECTION
```

### Результат
Эталонный путь работает:
```
goal → strategy → budget → MEETING_1 → [отказ] → payment → location → lpr → MEETING_2 → [отказ] → FINISH
```

### Теги
#bug-fix #planner #call_proposal_count #контрольная_точка

### 02.02.2026: Race condition (BUG-002)
**Проблема:** asyncio.create_task() → параллельная обработка → дубли
**Решение:** message_queue.py — lock + очередь, объединение сообщений
**Почему:** Lock = последовательность, очередь = все сообщения в одном ответе

### 02.02.2026: Холодные vs горячие — системная проблема
**Проблема:** RAG и промпт заточены под горячих. При холодной базе — неадекватное поведение
**Решение (план):** Две ветки warm/cold. Флаг lead_type. Для cold: АКТУАЛИЗАЦИЯ перед квалификацией
**Почему:** Два разных сценария требуют разную тональность и стратегию

### 02.02.2026: Честность про бота
**Проблема:** София врала что живой человек
**Решение:** "разумный ассистент" + предложить живого эксперта
**Почему:** Доверие важнее конверсии

### 03.02.2026: BUG-003 — обрезанные ответы GPT-5.2
**Проблема:** Ответ Софии обрезался на полуслове ("...или закры"). max_output_tokens=800.
**Причина:** GPT-5.2 reasoning модель тратит часть токенов на внутреннее рассуждение, оставляя мало для ответа.
**Решение:** Analyzer 300→1000, Generator 800→4000 + логирование stop_reason и детекция truncation.
**Урок:** Для reasoning моделей нужен 3-5x запас по max_output_tokens.

### 03.02.2026: Архитектура холодных лидов
**Проблема:** София обучена на горячих клиентах, но работает с холодной базой — клиенты не помнят заявку.
**Решение:** Трёхуровневая система:
1. lead_type (cold/warm/hot) в БД — устанавливается при outreach
2. Блок АКТУАЛИЗАЦИЯ в промпте — быстрая проверка актуальности перед квалификацией
3. Разные шаблоны приветствий в sofia_outreach.sh
**Принцип:** Cold → "ещё актуально?" (мягко). Warm → "удобно пообщаться?" (уверенно). Hot → "подготовили подборку" (конкретно).
**Источник:** Скрипт "Лидоруб" — таб "переписка" (мессенджер-сценарии для холодных).

### 03.02.2026: Лидоруб — ключевые паттерны для cold outreach
**Источник:** Google Sheets скрипт продаж (6 табов)
**Главные приёмы:**
- Объяснять ЗАЧЕМ спрашиваем: "объекты для инвестиций и для жизни сильно отличаются"
- При "пришлите цену": не отказывать, а мягко квалифицировать: "Конечно! Для себя или инвестиция?"
- При "позже/через полгода": узнать от чего зависит срок + предложить Telegram канал
- При "не помню заявку": не извиняться, а переформулировать: "В целом недвижимость интересна?"
**Не реализовано:** Follow-up сообщения (требуют таймеры — отдельная задача).

---

### 05-06.02.2026: Planner cleanup

**Проблема:** Planner (gpt-5.2) выбирал action и формировал action_context, но в generate_response() этот контекст нигде не читался. Лишний вызов API на каждое сообщение.

**Решение:** Полностью убрали Planner из message_processor.py. Теперь: Extractor → State → Signals (75 строк вместо 160).

**Почему:** Архитектура v3.0 "LLM с контекстом" — Generator принимает решения на основе state_summary + RAG + промпт, а не жёсткого action от Planner.

**Результат:** Экономия ~2-5 сек и ~$0.01-0.05 на сообщение.

---

### 05-06.02.2026: Баг save_lead_type в outreach

**Проблема:** outreach.sh сохранял lead_type в несуществующее поле `client_id` с неправильным ID. lead_type не доходил до Generator.

**Решение:** Исправили на `user_id = -(offset + chat_id)` по той же формуле что gateway.

**Почему:** Разные части системы должны использовать одинаковую формулу ID.

---

### 05-06.02.2026: Темы в Observer (Forum Topics)

**Проблема:** Все диалоги шли в одну ленту — невозможно читать, всё смешивается.

**Решение:** Включили темы в группе, каждый клиент = отдельная тема. Добавили таблицу observer_topics (phone → thread_id).

**Важно:** При включении тем группа становится супергруппой с новым ID. Нужно обновить OBSERVER_CHAT_ID в .env и sofia_outreach.sh.

---

### 05-06.02.2026: Формула user_id для Radist

**Правило:**
- Max: `user_id = -(1000000 + chat_id)`
- Telegram: `user_id = -(2000000 + chat_id)`
- WhatsApp: `user_id = -(3000000 + chat_id)`

**Почему:** chat_id от Radist положительный, а client_state изначально проектировался под отрицательные ID. Offset по каналам чтобы не было коллизий.

### 07.02.2026: Экспертная позиция — патч обработки возражений
**Проблема:** София соглашалась с каждым возражением ("убираем Анапу", "да, в Крыму опасно"), сужая воронку. Клиент Максим: из 4 регионов → 1, только полная оплата, 13 млн Сочи готовое с ремонтом = 0 вариантов.
**Решение:** Блок "ТЫ — ЭКСПЕРТ, А НЕ ОБСЛУГА". Формула: прими эмоцию → контраргумент → покажи выгоду → оставь открытым. 8 шаблонов, 3 запрета, принцип воронки (убрал регион = минус 30-40%).
**Почему:** Генератор без инструкций по умолчанию "помогает" клиенту — соглашается. Нужна явная установка на экспертизу.

### 07.02.2026: Бан Max — уроки массовой рассылки
**Проблема:** ~50+ сообщений за 30 минут → номер забанен навсегда. 29 чатов мертвы.
**Решение:** WABA как основной канал. Max — дополнительный (10-15/день, "напишите стоп", ротация).
**Почему:** Личный аккаунт + массовая рассылка = бан всегда. WABA — официальный API, бан невозможен при соблюдении правил.

### 07.02.2026: Коды ошибок Max
- CONTACT_NOT_FOUND (30003) — нет аккаунта, повторять бесполезно
- GENERIC_ERROR (30000) — рейтлимит, можно повторить
- UNABLE_TO_VALIDATE_PHONE (15001) — жёсткий лимит
- Паттерн: ~20 успешных → GENERIC_ERROR, ~50 → бан

### 07.02.2026: Названия секций в промпте — всегда проверяй
**Проблема:** Патч искал "ПРИНЦИПЫ ВЕДЕНИЯ ДИАЛОГА", реальная секция — "ПРИНЦИПЫ ОБЩЕНИЯ". Патч бы не сработал.
**Решение:** ВСЕГДА запрашивать cat sofia_prompt_v2.py перед патчем. Команда: grep -A1 "═══" sofia_prompt_v2.py

### 07.02.2026: WABA ограничения
- Не работает для Крыма, Донецка, Луганска, Турции (Meta)
- Лимит: 250 → 1000 → 10000 → безлимит (при хорошем quality score)
- Подписка Radist: ~5000₽/мес + 5-8₽ за исходящее
- Номер нельзя использовать одновременно в обычном WhatsApp и WABA
- Первое сообщение — только через одобренный шаблон (HSM), после ответа — 24ч свободный диалог

### 07.02.2026: Битрикс интеграция — два варианта
- **Вариант 1 (рекомендован):** Исходящий вебхук ONCRMLEADADD — Битрикс сам шлёт POST на наш URL. Безопаснее для них — не даём доступ к CRM, они контролируют что отправлять.
- **Вариант 2:** Входящий вебхук (API-ключ) — мы сами опрашиваем crm.lead.list. Видим ВСЕ лиды, они могут не захотеть.
- Endpoint: http://72.56.64.91:5001/api/bitrix/lead (route ещё НЕ создан в gateway)

### 07.02.2026: Radist.Online — нужен ли?
- Telegram бот (@humanAINeural_bot) может работать без Radist через Bot API бесплатно
- WABA можно подключить напрямую через 360Dialog без Radist
- Radist стоит ~5000₽/мес, даёт единое окно для каналов
- Решение отложено, требует детального анализа

---

### 10.02.2026: Web API для чат-виджета

**Задача:** Подключить виджет на сайте atlantis-invest.ru к реальной Софье.

**Решение:** Создан `web_api.py` — FastAPI сервер на порту 8080. Тот же пайплайн что Telegram и Radist: process_message() → Analyzer → RAG → Generator. Никакие существующие файлы не тронуты.

**Инфраструктура:**
- DNS: `api.atlantis-invest.ru` → 72.56.64.91 (A-запись в reg.ru)
- Nginx reverse proxy: 443 → localhost:8080
- SSL: Let's Encrypt (автообновление certbot)
- Systemd: `sofia-web-api.service`
- CORS: atlantis-invest.ru

**Веб-юзеры:** user_id = 9_000_000 + hash(session_id) % 1_000_000. Не пересекаются с Telegram ID.

**Решение по HTTPS:** Сайт на HTTPS → браузер блокирует HTTP-запросы (mixed content). Поэтому обязателен SSL на API. Субдомен + Let's Encrypt — самый простой путь.

---

### 10.02.2026: BUG-004 — Generator знает, Extractor забывает

**Проблема:** Клиент написал "lkz bydtcnbwbq" (EN-раскладка = "для инвестиций"). Generator понял контекст и ответил "Поняла, для инвестиций". Но Extractor не распарсил транслит → goal=null в state. При следующем сообщении Generator увидел пустой goal → переспросил.

**Причина:** Generator и Extractor — два независимых LLM-вызова. Generator видит всю историю и понимает контекст. Extractor парсит конкретное сообщение буквально.

**Варианты решения:**
- (А) Автоконверсия EN→RU раскладки перед Extractor (быстро, 10 строк, но покрывает только русский в англ. раскладке)
- (Б) Extractor/Analyzer учитывает предыдущий ответ Generator (правильнее, но сложнее)

**Статус:** НЕ ИСПРАВЛЕН

### 13.02.2026: BUG-005 — Greeting должен быть в истории
**Проблема:** София спрашивает в greeting "Какая цель: отдых / доход / комбинированно?", клиент отвечает "доход", но LLM не видит вопроса → интерпретирует "доход" как вопрос о доходности
**Решение:** save_message() greeting в create_session() перед возвратом клиенту
**Почему:** LLM должен видеть свои собственные вопросы чтобы правильно интерпретировать ответы

### 13.02.2026: Веб-стратегия — ценность вперёд, вопросы позже
**Проблема:** Мессенджер-стиль (6-7 вопросов квалификации) теряет анонимных web-клиентов
**Решение:** Макс 3 вопроса (цель/бюджет/оплата), давать ценность сразу, контакт обязателен перед [END]
**Почему:** Web-клиент анонимный, может закрыть вкладку. Нужно вовлечь ценностью, потом квалифицировать

### 15.02.2026: Финансовая экспертиза как инструмент вовлечения
**Проблема:** Клиенты сравнивают с депозитом, София не могла дать конкретику
**Решение:** Готовый расчёт в промпте: 17 млн → недвижимость 41 млн (+139%) vs депозит 24 млн (+40%)
**Почему:** Бесплатная экспертиза = доверие. "Если бесплатно дают такой уровень, с ними можно работать"

### 15.02.2026: Критическая ошибка цен — 5.2 vs 16.8 млн
**Проблема:** В промпте были цены "от 5.2 млн", реальные — от 16.8 млн
**Решение:** Полный прайс по типам из актуальной базы застройщика
**Почему:** Неверные цены = потеря доверия при первом же контакте с менеджером

### 16.02.2026: localStorage persistence для веб-виджета
**Проблема:** Клиент закрыл вкладку → session_id потерян → новая сессия, София не помнит контекст
**Решение:** localStorage на клиенте + web_sessions таблица + /api/session/resume эндпоинт
**Почему:** Без persistence каждый визит = новый клиент. София теряет весь контекст

### 16.02.2026: Стабильные user_id вместо hash()
**Проблема:** hash(session_id) давал случайные числа, при рестарте Python менял seed → другой user_id → коллизии
**Решение:** Автоинкремент от MAX(existing)+1, seed-запись _seed_skip сдвигает за 9971140
**Почему:** Коллизия user_id = смешанные переписки двух разных клиентов

### 16.02.2026: Сбор контакта — материалы или созвон
**Проблема:** София спрашивала "WhatsApp или Telegram?" но WABA не подключен
**Решение:** Выбор "отправить материалы или созвониться". Материалы → @username, созвон → телефон. Получила контакт → [END]
**Почему:** Предлагать канал которого нет = потеря доверия + лишние шаги
