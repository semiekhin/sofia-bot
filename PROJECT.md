# PROJECT.md — Sofia Bot (Hybrid System)

> Единая точка входа. Этот файл содержит ВСЁ для продолжения работы.
> Последнее обновление: 2025-12-29

---

## 🤖 CLAUDE: СТАРТ СЕССИИ

### 1. Прочитай HANDOFF (внизу документа)
Пойми: что горит, что сделали, что дальше.

### 2. Загрузи актуальный код
```
https://raw.githubusercontent.com/semiekhin/sofia-bot/main/PROJECT.md
```

### 3. Скажи
"Изучил PROJECT.md. [Состояние из HANDOFF]. Что делаем?"

### Контрольные слова
| Команда | Действие |
|---------|----------|
| `актуализируем` | Обновить PROJECT.md → commit → push |
| `переходим в новый чат` | Актуализация + выдать ссылку на PROJECT.md |

---

## 📋 QUICK INFO

| Параметр | Значение |
|----------|----------|
| **Проект** | Sofia Bot — AI-продажник недвижимости |
| **Компания** | Oazis Estate |
| **Архитектура** | Гибридная: код (логика) + LLM (текст) |
| **Стек** | Python 3.12, python-telegram-bot, OpenAI GPT-5.2, SQLite |
| **Версия** | 2.1 (hybrid) |
| **Статус** | ✅ Production |

### Ссылки

| Ресурс | URL |
|--------|-----|
| **GitHub** | https://github.com/semiekhin/sofia-bot |
| **Сервер** | `ssh -p 2222 root@72.56.64.91` |
| **Бот (hybrid)** | @humanAINeural_bot |
| **Бот (claude A/B)** | @humanClaudeAINeural_bot |

### Сервисы на сервере

| Сервис | Статус | Описание |
|--------|--------|----------|
| `sofia-hybrid` | 🟢 ACTIVE | Гибридная система v2.1 |
| `sofia-bot` | 🔴 STOPPED | Legacy GPT (отключен) |
| `sofia-claude` | 🟢 ACTIVE | Claude версия для A/B |

---

## 🔐 СЕКРЕТЫ

**Расположение:** `/opt/sofia-hybrid/.env` (chmod 600, не в git)

| Переменная | Описание |
|------------|----------|
| `OPENAI_API_KEY` | API ключ OpenAI |
| `TELEGRAM_BOT_TOKEN` | Токен бота @humanAINeural_bot |
| `ADMIN_CHAT_ID` | 512319063 |

---

## 🏗️ АРХИТЕКТУРА (HYBRID v2.1)

### Концепция
**Проблема:** LLM не умеет считать и следовать жёстким правилам.
**Решение:** Код контролирует логику, LLM только генерирует текст.

```
Сообщение клиента
       ↓
КОД: analyze_history() → счётчики
       ↓
КОД: decide_action() → ЖЁСТКАЯ ЛОГИКА
       ↓
LLM: generate_response() → текст
       ↓
КОД: страховка (вырезает ? если запрещено)
       ↓
Ответ клиенту
```

### Жёсткие лимиты (в коде, не в промпте!)

| Условие | Действие |
|---------|----------|
| Раздражение клиента | → SEND_MATERIALS, без вопросов |
| 2+ раз "скинь" | → SEND_MATERIALS, без вопросов |
| Вопрос после "скинь" и снова "скинь" | → SEND_MATERIALS, без вопросов |
| 14+ сообщений | → SEND_MATERIALS, без вопросов |
| 2+ отказа от созвона | → SEND_MATERIALS, без вопросов |
| 3+ ответа "без разницы" | → SEND_MATERIALS, без вопросов |

### Конфигурация моделей

| Режим | Модель | API | Reasoning |
|-------|--------|-----|-----------|
| `gpt-4o` | gpt-4o | chat.completions | ❌ |
| `gpt-5.2` | gpt-5.2 | responses | ❌ |
| `gpt-5.2-reasoning` | gpt-5.2 | responses | ✅ xhigh |

**Переключение:** `/model <режим>` в боте или через БД.

### Файлы системы

```
/opt/sofia-hybrid/
├── bot_server_hybrid.py   # Telegram бот
├── sofia_hybrid.py        # Логика: детекторы, анализ, генерация
├── sofia_prompt.py        # Промпт v4.1 (полный)
├── sofia_hybrid.db        # SQLite: диалоги, настройки, debug
└── .env                   # Секреты
```

---

## ⚙️ УПРАВЛЕНИЕ

### Гибридный бот (основной)
```bash
systemctl status sofia-hybrid
systemctl restart sofia-hybrid
journalctl -u sofia-hybrid -f
journalctl -u sofia-hybrid -n 50 --no-pager
```

### Переключение модели
```bash
# Через бота
/model gpt-5.2
/model gpt-5.2-reasoning
/model gpt-4o

# Через БД
sqlite3 /opt/sofia-hybrid/sofia_hybrid.db "INSERT OR REPLACE INTO settings (key, value) VALUES ('model_mode', 'gpt-5.2');"
systemctl restart sofia-hybrid
```

### Debug команды в боте
```
/start   — начать диалог
/reset   — сбросить диалог
/status  — счётчики диалога
/debug   — последние 5 решений системы
/model   — текущая модель / переключить
```

### Legacy (отключен)
```bash
systemctl status sofia-bot      # должен быть inactive
systemctl status sofia-claude   # A/B тест с Claude
```

---

## 📊 МЕТРИКИ

### Текущее состояние (2025-12-29)

| Метрика | Значение | Цель | Статус |
|---------|----------|------|--------|
| GOOD rate | 57% | 80% | 🔴 |
| Система | Hybrid v2.1 | - | ✅ |
| Модель | gpt-5.2-reasoning | - | ✅ |

### Известные проблемы
- Бот иногда задаёт 2 вопроса (промпт говорит "один", но LLM игнорирует)
- Имя клиента в каждом сообщении (раздражает)
- Детектор раздражения не ловит "зачем вы..."

---

## 📝 TASKS

### 🔥 Сейчас
- [ ] Доработать детекторы: добавить "зачем вы", "почему вы"
- [ ] Убрать имя клиента из каждого сообщения
- [ ] Тестировать hybrid vs legacy
- [ ] Закоммитить гибридную систему в репо

### 📋 Backlog
- [ ] GOOD rate 57% → 80%
- [ ] A/B тест: GPT-5.2 vs Claude
- [ ] Fine-tuning при 500+ GOOD диалогов

### ✅ Сделано
- [x] v2.1: Гибридная система с жёсткой логикой
- [x] v2.0: Переключение моделей (gpt-4o, gpt-5.2, gpt-5.2-reasoning)
- [x] Интеграция полного промпта v4.1
- [x] Страховка: код вырезает вопросы если запрещено
- [x] Debug команды (/status, /debug, /model)

---

## 📜 CHANGELOG

### v2.1 (2025-12-29)
- Гибридная система: код (логика) + LLM (текст)
- Жёсткие лимиты в коде (не в промпте)
- Переключение моделей: gpt-4o, gpt-5.2, gpt-5.2-reasoning
- Интеграция промпта v4.1
- Детекторы: раздражение, "скинь", отказ от созвона, нейтральные ответы
- Страховка: вырезание вопросов если запрещено

### v1.0.6 (2025-12-28)
- PROJECT.md + DEV_STANDARD.md

### v1.0.5 (2025-12-20)
- Skill-based assessment

---

## 🔄 HANDOFF

> Последняя сессия: 2025-12-29

### Что сделали
- Развернули гибридную систему v2.1 на @humanAINeural_bot
- Отключили legacy sofia-bot
- Добавили переключение моделей (gpt-4o, gpt-5.2, gpt-5.2-reasoning)
- Исправили вызов GPT-5.2 API (убрали temperature, добавили instructions)
- Подключили полный промпт v4.1
- Добавили колонку model_mode в БД
- Протестировали — бот работает

### Что не работает идеально
- Бот иногда задаёт 2 вопроса в одном сообщении
- Имя клиента в каждом сообщении
- Детектор не ловит "зачем вы постоянно..."

### Что не успели
- Закоммитить гибридную систему в GitHub
- Добавить паттерны "зачем вы", "почему вы" в детекторы
- Обновить .gitignore

### Следующий шаг
1. Добавить детекторы "зачем вы", "почему вы"
2. Убрать имя из каждого сообщения (в промпте)
3. Закоммитить всё в репо
4. Продолжить тестирование

### Активные сервисы
```
sofia-hybrid  → 🟢 @humanAINeural_bot (основной)
sofia-claude  → 🟢 @humanClaudeAINeural_bot (A/B)
sofia-bot     → 🔴 отключен
```

### Текущая модель
`gpt-5.2-reasoning` (с xhigh)

---

*Последнее обновление: 2025-12-29*
