# PROJECT.md — Sofia Bot (Hybrid System)

> Единая точка входа. Этот файл содержит ВСЁ для продолжения работы.
> Последнее обновление: 2025-12-29

---

## 🤖 CLAUDE: СТАРТ СЕССИИ

### 1. Прочитай HANDOFF (внизу документа)
Пойми: что горит, что сделали, что дальше.

### 2. Скажи
"Изучил PROJECT.md. [Состояние из HANDOFF]. Что делаем?"

### Контрольные слова
| Команда | Действие |
|---------|----------|
| `актуализируем` | Обновить PROJECT.md → commit → push |
| `переходим в новый чат` | Актуализация + выдать ссылку на PROJECT.md |

---

## 📋 QUICK INFO

| Параметр | Значение |
|----------|----------|
| **Проект** | Sofia Bot — AI-продажник недвижимости |
| **Компания** | Oazis Estate |
| **Архитектура** | Гибридная: код (логика) + LLM (текст) |
| **Стек** | Python 3.12, python-telegram-bot, OpenAI GPT-5.2, SQLite |
| **Версия** | 2.1 (hybrid) |
| **Статус** | ✅ Production |

### Ссылки

| Ресурс | URL |
|--------|-----|
| **GitHub** | https://github.com/semiekhin/sofia-bot |
| **Сервер** | `ssh -p 2222 root@72.56.64.91` |
| **Бот (hybrid)** | @humanAINeural_bot |
| **Бот (claude A/B)** | @humanClaudeAINeural_bot |

### Сервисы на сервере

| Сервис | Статус | Описание |
|--------|--------|----------|
| `sofia-hybrid` | 🟢 ACTIVE | Гибридная система v2.1, GPT-5.2-reasoning |
| `sofia-bot` | 🔴 STOPPED | Legacy GPT (отключен) |
| `sofia-claude` | 🟢 ACTIVE | Claude версия для A/B |

---

## 🔐 СЕКРЕТЫ

**Расположение:** `/opt/sofia-hybrid/.env` (chmod 600, не в git)

| Переменная | Описание |
|------------|----------|
| `OPENAI_API_KEY` | API ключ OpenAI |
| `TELEGRAM_BOT_TOKEN` | Токен бота @humanAINeural_bot |
| `ADMIN_CHAT_ID` | 512319063 |

---

## 🏗️ АРХИТЕКТУРА (HYBRID v2.1)

### Концепция
**Проблема:** LLM не умеет считать и следовать жёстким правилам.
**Решение:** Код контролирует логику, LLM только генерирует текст.

```
Сообщение клиента
       ↓
КОД: analyze_history() → счётчики
       ↓
КОД: decide_action() → ЖЁСТКАЯ ЛОГИКА
       ↓
LLM: generate_response() → текст
       ↓
КОД: страховка (вырезает ? если запрещено)
       ↓
Ответ клиенту
```

### Детекторы (паттерны в коде)

| Детектор | Что ловит | Файл |
|----------|-----------|------|
| `IRRITATED_PATTERNS` | Раздражение клиента | sofia_hybrid.py |
| `SEND_PATTERNS` | Просьбы "скинь варианты" | sofia_hybrid.py |
| `CALL_REJECT_PATTERNS` | Отказы от созвона | sofia_hybrid.py |
| `NEUTRAL_PATTERNS` | Ответы "без разницы" | sofia_hybrid.py |
| `CALL_AGREE_PATTERNS` | Согласие на созвон | sofia_hybrid.py |

**⚠️ ПРОБЛЕМА:** Паттерны сейчас — догадки, а не данные из реальных диалогов!

### Жёсткие лимиты (в коде)

| Условие | Действие |
|---------|----------|
| Раздражение клиента | → SEND_MATERIALS, без вопросов |
| 2+ раз "скинь" | → SEND_MATERIALS, без вопросов |
| 14+ сообщений | → SEND_MATERIALS, без вопросов |
| 2+ отказа от созвона | → SEND_MATERIALS, без вопросов |
| 3+ ответа "без разницы" | → SEND_MATERIALS, без вопросов |

### Конфигурация моделей

| Режим | Модель | API | Reasoning |
|-------|--------|-----|-----------|
| `gpt-4o` | gpt-4o | chat.completions | ❌ |
| `gpt-5.2` | gpt-5.2 | responses | ❌ |
| `gpt-5.2-reasoning` | gpt-5.2 | responses | ✅ xhigh |

**Текущий режим:** `gpt-5.2-reasoning`

### Файлы гибридной системы

```
/opt/sofia-hybrid/
├── bot_server_hybrid.py   # Telegram бот
├── sofia_hybrid.py        # Логика: детекторы, анализ, генерация
├── sofia_prompt.py        # Промпт v4.1 (полный)
├── sofia_hybrid.db        # SQLite: диалоги, настройки, debug
└── .env                   # Секреты
```

---

## 🛠️ ИНСТРУМЕНТЫ АНАЛИЗА ДИАЛОГОВ

### Скрипты

| Файл | Назначение |
|------|------------|
| `scripts/dialog_processor.py` | Парсинг RTF/TXT + эвристическая оценка + LLM-оценка |
| `scripts/batch_process.py` | Пакетная обработка папки диалогов |

### dialog_processor.py

**Функции:**
- `decode_rtf()` — декодирует RTF в текст
- `parse_dialog()` — извлекает сообщения (дата, время, автор, текст)
- `score_dialog()` — эвристическая оценка 0-10 (бесплатно)
- `llm_score_dialog()` — LLM-оценка по навыкам (платно, точно)
- `dialog_to_jsonl()` — конвертация для fine-tuning

**LLM-оценка возвращает:**
```json
{
  "qualification": {"score": 7, "good_for_training": true},
  "closing": {"score": 5, "good_for_training": false},
  "objection_handling": {"score": 8, "good_for_training": true},
  "strengths": ["..."],
  "mistakes": ["..."]
}
```

### batch_process.py

**Использование:**
```bash
# Только эвристики (быстро, бесплатно)
python batch_process.py ./dialogs/ --output dataset.jsonl

# Эвристики + LLM (точно, платно)
python batch_process.py ./dialogs/ --llm --output dataset.jsonl
```

**Выход:**
- `dataset.jsonl` — диалоги для fine-tuning
- `dataset_report.json` — полный отчёт с оценками

### Локальные диалоги
```
/Users/sergeysemiekhin/human bot/диалоги/
└── 49 RTF файлов с реальными диалогами
```

---

## ⚙️ УПРАВЛЕНИЕ

### Гибридный бот
```bash
systemctl status sofia-hybrid
systemctl restart sofia-hybrid
journalctl -u sofia-hybrid -f
```

### Переключение модели
```bash
/model gpt-5.2-reasoning  # в боте
```

### Debug команды в боте
```
/start   — начать диалог
/reset   — сбросить диалог
/status  — счётчики диалога
/debug   — последние решения системы
/model   — текущая модель
```

---

## 📊 МЕТРИКИ

| Метрика | Значение | Цель | Статус |
|---------|----------|------|--------|
| GOOD rate | 57% | 80% | 🔴 |
| Квалификация | 3.1/10 | ≥7 | 🔴 |
| Закрытие | 6.1/10 | ≥7 | 🟡 |
| Возражения | 4.2/10 | ≥7 | 🟡 |

---

## 📝 TASKS

### 🔥 Сейчас — Извлечение паттернов из диалогов
- [ ] Прогнать 49 диалогов через batch_process.py
- [ ] Извлечь реальные фразы раздражения → IRRITATED_PATTERNS
- [ ] Извлечь реальные просьбы "скинь" → SEND_PATTERNS
- [ ] Извлечь реальные отказы от созвона → CALL_REJECT_PATTERNS
- [ ] Обновить sofia_hybrid.py с новыми паттернами
- [ ] Протестировать

### 📋 Backlog
- [ ] GOOD rate 57% → 80%
- [ ] A/B тест: GPT-5.2 vs Claude
- [ ] Добавить node_modules/ в .gitignore
- [ ] Fine-tuning при 500+ GOOD диалогов

### ✅ Сделано (2025-12-29)
- [x] Гибридная система v2.1 развёрнута
- [x] Переключение моделей (gpt-4o, gpt-5.2, gpt-5.2-reasoning)
- [x] Интеграция полного промпта v4.1
- [x] Страховка: код вырезает вопросы если запрещено
- [x] PROJECT.md обновлён
- [x] Закоммичено в GitHub

---

## 📜 CHANGELOG

### v2.1 (2025-12-29)
- Гибридная система: код (логика) + LLM (текст)
- Жёсткие лимиты в коде (не в промпте)
- Переключение моделей: gpt-4o, gpt-5.2, gpt-5.2-reasoning
- Интеграция промпта v4.1
- Детекторы: раздражение, "скинь", отказ от созвона
- Страховка: вырезание вопросов если запрещено
- PROJECT.md полностью обновлён

### v1.0.6 (2025-12-28)
- PROJECT.md + DEV_STANDARD.md

### v1.0.5 (2025-12-20)
- Skill-based assessment

---

## 🔄 HANDOFF

> Последняя сессия: 2025-12-29

### Что сделали
1. Развернули гибридную систему v2.1 на @humanAINeural_bot
2. Исправили вызов GPT-5.2 API (instructions вместо system в messages)
3. Подключили полный промпт v4.1 из sofia_prompt.py
4. Протестировали — бот работает, но есть проблемы
5. Закоммитили в GitHub (v2.1)
6. Создали документацию Sofia_Hybrid_System.docx

### Что НЕ работает идеально
- Бот иногда задаёт 2 вопроса в одном сообщении
- Имя клиента в каждом сообщении (раздражает)
- Детектор раздражения не ловит "зачем вы...", "почему вы..."
- **Паттерны — догадки, а не данные из реальных диалогов**

### Следующий шаг — ИЗВЛЕЧЕНИЕ ПАТТЕРНОВ

**Цель:** Заполнить детекторы реальными фразами из диалогов

**План:**
```bash
# 1. На локальной машине
cd /Users/sergeysemiekhin/human\ bot/диалоги/

# 2. Прогнать через batch_process.py
python /opt/sofia-bot/scripts/batch_process.py . --llm --output patterns.jsonl

# 3. Из результатов извлечь:
#    - Фразы раздражения клиентов
#    - Фразы просьб "скинь"
#    - Фразы отказов от созвона
#    - Нейтральные ответы

# 4. Обновить в sofia_hybrid.py:
#    IRRITATED_PATTERNS = [...]
#    SEND_PATTERNS = [...]
#    CALL_REJECT_PATTERNS = [...]
#    NEUTRAL_PATTERNS = [...]
```

### Активные сервисы
```
sofia-hybrid  → 🟢 @humanAINeural_bot (gpt-5.2-reasoning)
sofia-claude  → 🟢 @humanClaudeAINeural_bot (A/B тест)
sofia-bot     → 🔴 отключен
```

### Файлы на сервере
```
/opt/sofia-hybrid/
├── sofia_hybrid.py        # ← сюда добавить новые паттерны
├── sofia_prompt.py        # промпт v4.1
├── bot_server_hybrid.py   # Telegram бот
└── sofia_hybrid.db        # БД
```

---

*Последнее обновление: 2025-12-29*
