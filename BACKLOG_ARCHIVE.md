# BACKLOG_ARCHIVE — Архив завершённых задач

> 📁 **Это архив секций "Завершено"** из BACKLOG.md, выгруженный 2026-04-29 (последнее обновление 2026-05-06).
> Активный backlog только с открытыми задачами P0/P1/P2/P3: [BACKLOG.md](BACKLOG.md).
> Записи сохранены as-is, в исходном порядке (свежее сверху).

## Завершено (06.05.2026)

- [x] **ATLANTIS_PROMPT_ADDON_POLISH_v1** — коммиты DEV `27cb23c4` (push origin/main `d53a4a47..27cb23c4`) + PROD `a8d0921e` (без push by-design). Микро-итерация после live-smoke виджет-Sofia 06.05 — две точечные правки текста `prompt_addon` в `config/source_objects.py` обоих репо: (a) в блок «ПЕРВАЯ РЕПЛИКА» добавлен абзац-запрет на упоминание агентства / бренда «Oazis Estate» — Sofia в smoke представилась «помощник отдела продаж Oazis Estate», что некорректно для виджета и мешает масштабированию; (b) в блок «ПРЕЗЕНТАЦИЯ (PDF)» добавлен абзац про нейтральную формулировку отправки и список 5 запрещённых жаргонизмов («накину», «держите», «лови», «кину», «скину») — Sofia в smoke написала «Накину вам презентацию», что двусмысленно («накину» = «повышу цену»). Длина addon 3464 → 3920 байт (+456). Runtime md5 значения addon DEV=PROD `4f08421d2e5cabd2b246b62be6fdc515` × 2. 5 рестартов 0 ERROR. Snapshots `/tmp/source_objects.py.{prod,dev}.before_addon_polish_20260506_082633`. Не было задачей в активном backlog — это продуктовая полировка после ATLANTIS_WIDGET_PROMPT_ADDON_v1 (05.05).

## Завершено (05.05.2026)

- [x] **ATLANTIS_ADDON_INFRA_PORTBACK** (P0 с 15.04, переоткрыт 28-29.04 при RIZALTA-deferred) — закрыто 05.05 коммитами PROD `f3213406` + DEV `25d81351`. В PROD `core/pipeline.py:206-207` добавлена ветка `if obj_config.get("prompt_addon"): system_prompt += f"\n\n{obj_config['prompt_addon']}"` (дословный портбэк DEV-образца с `:267-268`, две строки, не схлопывать). В обоих репо `config/source_objects.py` у atlantis-объекта появился ключ `"prompt_addon"` с новым текстом 3464 байт под виджет+Тильда одновременно. **Принципиальное изменение:** старый текст «Контакт клиента уже получен через форму на сайте» удалён — он был фактически ложным для виджет-клиента (`find_recent_atlantis_lead → None`). Новый текст не предполагает контакт ни для одного пути — Тильда-клиент в худшем случае подтвердит телефон ещё раз, что не ломает воронку. Голосовой `stream_voice_response` `:350-358` намеренно НЕ тронут (Atlantis в голос не использует, P3 backlog если когда-нибудь вернётся). 4 snapshots в `/tmp/*before_atl_addon_20260505_230514`. 5 рестартов (PROD radist+web-api+sofia-gpt + DEV radist-dev+web-api-dev) — 0 ERROR во всех журналах.
- [x] **source_objects.py prompt_addon разделение по каналам** (P1) — закрыто 05.05 другим способом, чем планировалось. Изначальный план: разные addon'ы для Telegram-Тильды (не запрашивать контакты) vs виджета (запрашивать телефон). Финальное решение Сергея: один addon для обоих путей, нейтральный относительно «контакт получен», ведёт квалификацию одинаково — Тильда-клиент в худшем случае ещё раз подтверждает телефон, виджет-клиент проходит штатный финал воронки с запросом телефона. Разделение по каналам в коде не нужно. Закрыто тем же коммитом `f3213406`/`25d81351`.
- [x] **ATLANTIS_WIDGET_BEHAVIOR_AUDIT_v1** (read-only sprint, без коммита) — 8 точек расхождения виджет-flow vs Тильда-flow vs `/start`-without-object с `file:line`, прогноз первых 4 реплик виджет-Sofia по сборке промпта, trifecta-таблица из 8 критических шагов. Аудит подтвердил гипотезу Сергея «виджет ломает логику» и точно локализовал причины: force-routing GREETING на репликах 1-2 (`core/pipeline.py:176-183`), object_context инжектируется (`:259-266`), prompt_addon «контакт уже получен» фактически ложен. Build Report сохранён в session log.

## Завершено (28-29.04.2026)

- [x] **ATLANTIS_WIDGETS_DOC_v1** — коммит `027a353e`. `docs/ATLANTIS_WIDGETS_STATE.md` (15.0 KB, 14.9 KB после трим до лимита) — справочник по двум независимым путям привлечения на Атлантисе: форма «Получить презентацию» (Тильда → `t.me/m/QinKZEsTNmRi`, SOURCE_ID=397 в Bitrix через webhook) и HTML-виджет «Чат с менеджером» (`widgets/atlantis_chat_widget.html`, `t.me/m/Fiw3ldhkN2My`, дефолтный `SOURCE_ID=504`). Парная правка scp-строки в Gitpull-блоке CLAUDE.md.
- [x] **RIZALTA_CONTEXT_EXTRACT_v1 + REBUILD_v2** — коммиты `cf104e7a` + `e25bc8f7`. `objects/rizalta_context.md` (102 строки / 13.4 KB). Первая версия — извлечение фактов только из `VOICE_SYSTEM_PROMPT_RIZALTA` (`core/pipeline.py:646-780`). Вторая итерация — расширение из `rag_training_data.json` (4 диалога-сделки RIZALTA, 109 совпадений по ключевым словам) с детальной фактологией: схема гарантированного дохода, 10-шаговая дорожная карта сделки, налоговая оптимизация, реферальная программа. Структура зеркалит `objects/atlantis_context.md`.
- [x] **RIZALTA_REGISTER_v1 (DEV)** — коммит `9947721d`. Регистрация объекта `"rizalta"` в `config/source_objects.py` с узкими keywords (без `белокуриха` чтобы избежать false-match). Двухуровневая защита от Bitrix: уровень 1 в `_is_radist_bitrix_session` (short-circuit для rizalta-сессий с `[BITRIX_SKIP]` логом в 2 caller-блоках), уровень 2 (defense-in-depth) в `create_or_update_lead` — payload подменяется на `TITLE/NAME="Тестовый лид"`, `PHONE="89181011091"` через `title_override`-параметр в `create_lead`, дедупликация пропускается, громкий `🚨 [BITRIX_RIZALTA_LEAK]` ERROR-уровня. Observer-tag `f"{display_name} · rizalta"` через прокинутый `source_object` параметр в `notify_observer` + `get_or_create_topic`. Helper `_get_source_object(user_id)` в radist_gateway.
- [x] **RIZALTA_TEST_DEV_v1** — 4 автотеста PASS перед PROD-деплоем: `detect_source` 6 кейсов (включая защиту от false-match по «Белокуриха»), `_is_radist_bitrix_session` 4 кейса, level-2 fallback с моком `aiohttp.ClientSession` (проверено что `find_lead_by_phone` НЕ зовётся, payload подменён, лог `🚨 [BITRIX_RIZALTA_LEAK]`), Observer `get_or_create_topic` 3 кейса формата имени темы.
- [x] **RIZALTA_DEPLOY_PROD_v1** — коммит `3be32d68` PROD. Split-deploy: `core/bitrix.py` + `sofia_radist_gateway.py` через `git apply --index` (DEV ≡ PROD до коммита, чисто), `objects/rizalta_context.md` через `cp DEV → PROD`, `config/source_objects.py` ручная вставка только rizalta-блока через `Edit` (drift PROD без `prompt_addon`/black). Снапшоты `/tmp/*.prod.before_rizalta_deploy_20260429_095745`. Рестарт всех трёх сервисов (sofia-radist + sofia-web-api + sofia-gpt) — оркестратор настоял на полном покрытии level-2 во всех каналах. ERROR-grep за 2 минуты после рестарта — 0 строк. PROD detect_source-тесты прошли.
- [x] **RIZALTA_OBSERVER_TOGGLE_v1** — коммит `9f2d35bf` PROD. Env-флаг `RIZALTA_OBSERVER_ENABLED` (default `"false"`) в `sofia_radist_gateway.py` + `.env` (gitignored). Guard внутри `notify_observer`: для rizalta-сессий пишет `🚫 [OBSERVER_SKIP] source=rizalta reason=disabled_by_env phone=<phone>` и выходит. Atlantis и не-source-сессии без изменений. Включение обратно — 2 sed+restart команды.
- [x] **SERGEY_STATE_RESET_v1 + RADIST_HISTORY_SOURCE_RECON_v1** — без коммитов (data ops + read-only разведка). Чистка state Сергея (user_id `-40076372`, chat_id `38076372`, tg_username `sergey_7in`): `client_state` + `messages` + `radist_leads` (последний имел привязку к историческому Atlantis-лиду 265944 от 15.04). После второй чистки Sofia всё равно тянула старый контекст — read-only расследование показало что `sofia_radist_gateway.get_history()` читает из таблицы `radist_messages` (по chat_id, не user_id), которую первая one-liner-чистка пропустила. Урок: bot_server/web_api в `messages` (по user_id), Radist в `radist_messages` (по chat_id) — universal cleanup по user_id неполный.
- [x] **RIZALTA_CONTEXT_INVESTMENT_ONLY_v1** — коммит `4d79a8ae` DEV + `a5820989` PROD. После боевого smoke-теста Sofia предложила «для себя или для инвестиции» — структурный дефект позиционирования. Карточка поправлена: «исключительно инвестиционный продукт, не для личного проживания», новый раздел «Личное проживание собственника не предусмотрено», старый раздел «Спецусловия для собственников» (с пунктом «2 недели весной/осенью со скидкой 30%») удалён, 2 фактических пункта перенесены (приёмка → «Условия оплаты», перепродажа → «Юридическое»). Новый пункт первым в «Инструкциях для диалога»: «не предлагай RIZALTA для себя или для инвестиции». DEV ≡ PROD по md5 (`56a3303c06e1...`). Снапшот `/tmp/rizalta_context.md.prod.before_20260429_120015`.

## Завершено (21.04.2026)

- [x] **START_SESSION_PARITY** — закрыта парная правка к коммиту `af11212f` от 19.04. Сергей обновил локальный `~/projects_claude/start-session.sh` на Mac: добавлены 4 scp-строки и соответствующие `cat` в `collect_context()` для `docs/PILOT_WORKSTATION_RUNBOOK.md`, `docs/LESSONS_LEARNED.md`, `docs/VOICE_ARCHITECTURE.md`, `docs/VOICE_TECH_STACK_FULL.md`. Локальный скрипт теперь полностью зеркалит Gitpull блок CLAUDE.md (13 файлов). Claude.ai следующей сессии получит operator runbook, уроки, voice-архитектуру и полный voice tech stack.
- [x] **DIAL_PY_METRICS_v1** — коммиты `8ac8233f` + `f28cd748`. Per-turn EOU / LLM / TTS-first-chunk метрики aggregированные на триггере `⏱️ Turn timings:`, формат `[MM:SS] METRICS: eou=Nms llm=Nms tts=Nms` после каждой SOFIA-реплики. Timers `[MM:SS]` от call_start для USER/SOFIA/METRICS/AUTO_HANGUP; `[HH:MM:SS]` wall clock для Ringing/Answered/Ended. Post-call: `Recording:` путь + `Download (run from your Mac): scp ...` one-liner (gate ANSWERED+billsec≥5). `f28cd748` fix: recording path из `CDR.lastdata` (CALL_UUID RFC-4122), не `CDR.uniqueid` — два разных UUID в Asterisk.
- [x] **DIAL_LIVE_LISTEN_v1** — коммит `f501d719`. `bin/listen_live.sh` + `inotify-tools 3.22.6.0` на VPS. `inotifywait -e create` на `/var/lib/asterisk/recordings/YYYY-MM-DD/` + `tail -c +0 -F <wav>` + exit after 20с idle growth. Mac-side: `while true; do ssh sofia-voice 'listen_live.sh' | ffplay -nodisp -autoexit -f s16le -ar 8000 -ch_layout mono -; done`. SSH ключ обязателен (fix via `ssh-copy-id sofia-voice` на Mac).
- [x] **EXECSTOP_AUDIOSOCKET_CLEANUP_v1** — коммит `37e099a5`. `bin/hangup_audiosocket.sh` + `ExecStop=` в `sofia-voice.service`. Защита от orphan AudioSocket root-cause 20.04. Smoke: restart на активном канале → через 3с 0 orphan, `sofia-voice-hangup: hangup PJSIP/...` в syslog, MainPID 24230→42525 auto-restart. Part 2 (graceful hangup клиентом) — ExecStop не вызван, disposition=ANSWERED. Дисциплинарное правило «no restart mid-call» остаётся.
- [x] **PILOT_OPERATOR_RUNBOOK** — коммит `2304b075`. CLAUDE.md § «Рабочая станция пилота (three-terminal workflow)» с T1 listen_live (ffplay s16le 8k mono), T2 dial.py, T3 scp download.
- [x] **DIAL_PY_CHANNEL_GIVEUP_v1** — коммиты `38c42abf` (v1 flawed) + `75082b73` (v2 working). Root cause hang'a dial.py на invalid/unanswered номерах: Asterisk не создаёт CDR для не-ANSWERED outgoing (несмотря на дефолтную доку «outgoing always logged»), `wait_for_cdr` polling до 600с. Разведка: `cdr.conf unanswered=yes` не помогло бы — дефолт применяется, наш случай pre-channel failure. Fix v1 (`is not None` → channel_ever_seen) провалился: Asterisk на 1–2с создаёт channel в state `Ring`/`Other` даже при silent reject. Fix v2 требует 3 consecutive Up/Ringing polls (~3с sustained) перед `channel_ever_seen = True`. Smoke silent failure 88000000000 — **exit 35.4с** (было 600с); regression 79181011091 — ANSWERED 30с billsec ✓. Snapshots `/tmp/dial.py.before_channel_giveup_20260421_114322` (v1), `/tmp/dial.py.before_channel_giveup_v2_20260421_121732` (v2).
- [x] **TELFIN_AUTODIAL_RECON** — web-research 6 источников. Вердикт: НЕЯСНО из public docs, поднят как P2 с ожиданием ответа менеджера. Детали выше в P2.
- [x] **Pilot runbook doc** — новый `docs/PILOT_WORKSTATION_RUNBOOK.md` (workstation commands + состояние 21.04 + 6 особенностей). SESSION_LOG обновлён с детальной хронологией.

## Завершено (20.04.2026)

- [x] **VPS_UPGRADE_2VCPU** — sofia-voice 1→2 vCPU, 4 GB RAM (Timeweb Premium NVMe, ~1000₽/мес). nproc=2, load 0.00, RAM 3.2 GB free после boot. **Гипотеза не подтвердилась:** 1 vCPU не был виновником проглатываний. Настоящая причина — AudioSocket buffer overflow (см. ниже).
- [x] **VOICE_SPEAK_INSTRUMENTATION_v1** — добавлена per-chunk timing инструментация в `_speak` (~20 строк). Собрано 8600+ CHUNK-записей с 4 звонков (sleep_overrun / send_dur / inter_chunk_gap distribution). Снята в session-end коммите после локализации корневой проблемы. Snapshot `/tmp/voice_asterisk.py.before_instr_20260420_041752` на VPS.
- [x] **TTS_AB_MATRIX_v1_для_8ad55f5b** — 6 дополнительных curl-WAV (reply5/reply8 × baseline/48k/jane) плюс MixMonitor WAV звонка 8ad55f5b. Все скачаны в `/tmp/pilot_recordings_18_04/call_8ad55f5b_*`. 48kHz размер = 6× baseline (sanity check Yandex действительно возвращает 48kHz). Подтвердило: Yandex TTS не источник проглатывания ни на 8kHz, ни на 48k→soxr.
- [x] **CALL_8AD55F5B_CHUNK_ANALYSIS + INTER_CHUNK_GAP_ANALYSIS** — статистика 4373 чанков звонка 8ad55f5b. sleep_overrun median 947us p99 2147us, send_dur max 458us. Первый вывод (неверный): «Python чист, копать Asterisk». Пересмотрен после silent test показал детерминированные dropouts в статичном PCM-файле.
- [x] **docs/VOICE_TECH_STACK_FULL.md** — новый файл 49KB (719 строк). Полный технический стек voice pipeline от SIP INVITE до звука клиента: ASCII-диаграмма, 19 шагов, 25 параметров с `file:line`, таблица 27 файлов, 2 приложения (навигация по коду + diagnostic команды). Коммичен в составе `7f65fadd`.
- [x] **SILENT_TEST_DIAGNOSIS_b3cd88c8** — звонок 65с где Сергей молчал, услышал проглатывания **только в greeting**. Ключевая находка: greeting идёт через `_speak_pcm` из статичного PCM-файла (198318 bytes, md5 `8e553237010691bb13f92f900a11d1b0`). Детерминированный дефект, не связан с нагрузкой / STT / LLM. Побайтовое сравнение source vs MixMonitor: correlation **0.034** (шум), 209 loud→silent dropout-окон 10ms, 277 attenuation >5×. Путь виновника сузился до `_speak_pcm → AudioSocket → Asterisk core → MixMonitor`.
- [x] **VOICE_PACING_FIX_v1** — коммит `8f3eeaf0`. Корневая причина: `asyncio.sleep(0.018)` в обеих `_speak`/`_speak_pcm` функциях создавал wall_per_chunk ~19.1ms против 20ms audio rate → AudioSocket buffer overflow на Asterisk-side → drop'ы фреймов. Три итерации: (1) 18→20ms symmetric (greeting чист correlation 0.987, но в активном разговоре underrun 5% и stutter на каждом слове), (2) откат в сторону **асимметричного** фикса `_speak=0.019 / _speak_pcm=0.020` — учитывает разный event loop overhead (855us под параллельным gRPC+Silero vs 610us без нагрузки). Контрольный звонок `dff69ac4` (126с, 7 реплик, 3395 чанков): per-chunk wall 19 980us (цель 20 000), inter-chunk gap median **20 000us ровно**, 0 dropouts, 0 проглатываний в TTS. PROD не тронут (voice-компонентов в `/opt/sofia-gpt/` нет).
- [x] **VOICE_PACING_DOCS** — коммит `7f65fadd`. Документация обновлена: `VOICE_TECH_STACK_FULL.md` раздел 10 (pacing перемещён из «открыто» в «проверено+решено» с полными метриками), ASCII-диаграмма + шаг 14 + таблицы параметров разделов 5/6/7 обновлены. `VOICE_ARCHITECTURE.md` блок «Pacing fix (20.04)» с объяснением асимметрии.
- [x] **CHUNK_INSTRUMENTATION_CLEANUP** — снята временная инструментация из `_speak` (~20 строк, включая SLEEP_OVERRUN warning). Возврат к исходной структуре loop'ов + финальное pacing значение 0.019. Git diff только 3 изменённые строки `0.018 → 0.020/0.019/0.019`, 0 residuals. Snapshot `/tmp/voice_asterisk.py.before_cleanup_20260420_071727` на VPS.

## Завершено (19.04.2026)

- [x] **CLAUDE_MD_SPLIT_v1** — CLAUDE.md 57k→22k (−60 %) через split в два docs-файла. `docs/LESSONS_LEARNED.md` (36 прецедентных уроков 21.03–18.04 дословно с SHA/UUID/датами) и `docs/VOICE_ARCHITECTURE.md` (полная voice-архитектура — AudioSocket, Silero VAD v4, Yandex STT/TTS, Phase 1 VLAT-07, auto-hangup, MixMonitor, VOICE_PROMPT_MODE). В CLAUDE.md остались summary-блоки с явными ссылками «Полный текст: docs/FILENAME.md». Gitpull дополнен scp-командами на оба новых файла. Коммит `af11212f`.
- [x] **BITRIX_LEAD_RESCUE_RECON + WEBHOOK_NGINX_RECON** — инцидент 19.04, лид 266638 Elena +79124753743. Разведка: Sofia трижды финализировала лид через `set_lead_assigned(24932)+BP 152` несмотря на ручной перехват менеджером (Bitrix ASSIGNED=19622). Root cause: Bitrix webhook ONCRMLEADUPDATE не настроен — 0 hit на `/api/bitrix/webhook` за 14 дней, `manager_active` ни разу не установился. Endpoint полностью живой и доступен снаружи (HTTPS 200, Let's Encrypt, nginx `sofia-api.conf` proxy_pass `127.0.0.1:8080`, UFW 443 open). Мяч на стороне Bitrix. Решение через stage-guard (ниже).
- [x] **BITRIX_STAGE_GUARD_v1 (DEV + PROD)** — функция `get_lead_status(lead_id) -> str | None` в `core/bitrix.py`, fail-safe (None при любой ошибке API). Встроена в 3 точки: `finalize_lead` (единая для всех каналов), `radist_send_reminder` (2 свежих вызова — перед отправкой + перед finalize после sleep), `_radist_finalize_timeout`. Skip-ветка → `find_user_id_by_lead` → `set_manager_active(True)` → log `[FINALIZE_SKIP|REMINDER_SKIP|TIMEOUT_SKIP]` → return. `update_lead` безопасна, не проверяет. DEV `441dec63`, PROD `7e08e9a6` (format-patch clean apply, 3 сервиса перезапущены без ERROR). Smoke: `get_lead_status(266638)` → `"18"` ✓.
- [x] **PILOT_INVENTORY_19_04** — инвентаризация `/var/lib/asterisk/recordings/` + CDR + journalctl за 18-19.04. Факт: 9 WAV (9.98 MB) за 18.04, папки 2026-04-19 нет, последний файл 18.04 17:32 MSK. 13 inbound от 4 уникальных номеров. AUTO_HANGUP 14/14 detect→exec 0 cancel. EOU_WAIT median 1035мс ≈ baseline 1036мс (Phase 1 работает). 0 реальных ошибок, 6 HALLUC_TRUNCATED (штатно), 4 `[END]`. Сервисы active. После 18.04 17:32 MSK пилот-активности нет ~24ч.
- [x] **AUDIO_DROPOUT_8a7c4983** — разведка звонка `8a7c4983` (+79189007826, 124с). Yandex PCM полный (bytes=duration до сэмпла), `_speak` отправил целиком, 0 BARGE-IN/cancel/HALLUC. Артефакты «безопасном»/«управляющую» — на стыках длинных слов в середине реплик, не в начале. Гипотеза (E) coarticulation, не наша сторона. Побочное: `_speak` не логирует per-chunk (создан P1 пункт).
- [x] **AUDIO_DROPOUT_94ea75b1** — разведка звонка `94ea75b1` (+79160322905, 69.5с). Найдено отличие от 8a7c4983: 1 false-positive BARGE-IN на reply #2 через 497мс после старта TTS (Silero VAD threshold=0.5, 200мс cumulative на TTS-echo). Reply #2 сыграла 655мс из 10.79с, **10.1с аудио потеряно**, не возобновляется. Клиент реальный вопрос задал через 4.7с (не речь). Плотность artefact-кандидатов 0.46/с идентична 8a7c4983. Побочное: создан P1 пункт про Silero threshold tuning.
- [x] **TTS_AB_MATRIX_v1** — сгенерировано 12 WAV через прямой curl на Yandex TTS: 4 проблемных текста × 3 варианта (baseline alena 8kHz / alena 48kHz→8kHz soxr / premium jane 8kHz). Идентично `synthesize_tts_yandex` + `add_ssml_breaks`. Все 12 файлов `pcm_s16le/8000/mono`. Файлы в `/tmp/pilot_recordings_18_04/ab_*.wav`. **Вердикт Сергея после прослушивания: все три варианта звучат чисто, проглатывания нет ни в одном** → проблема **не в Yandex TTS**.
- [x] **MIXMONITOR_DISABLE_TEST** — эксперимент на живом звонке Сергея (+79310091644, ~1-2 мин). Snapshot `extensions.conf` → закомментированы 2 MixMonitor строки → `dialplan reload` (без restart) → звонок Сергея про Белокуриху/Атлантис → восстановление через `cp` из snapshot → diff пустой, md5 совпал. PID Asterisk 698 и sofia-voice 161279 не менялись. **Субъективный вердикт Сергея: «немного лучше, но не решено»** — MixMonitor часть проблемы, но не вся. Новый симптом зафиксирован: **щелчки в аудио Sofia** (jitter / CPU contention). Главный вывод дня: 1 vCPU VPS — основной подозреваемый, апгрейд поднят в P0 на 20.04.

## Завершено (18.04.2026)

- [x] **VLAT-07 Phase 1 — `max_pause_between_words_hint_ms=700`** — env-gated (коммит `393eb062`). EOU_WAIT median **1778→1036мс (−742мс, −42 %)**, p95 2148→1446мс. Targets оркестратора hit с margin. Рекомендация по Phase 2/3 (ExternalEouClassifier, semantic EOU) — не делать без метрик пилота (docs/VLAT_07_DEEP_DIVE + PHASE1_RESULTS).
- [x] **EOU instrumentation в yandex_stt_grpc.py** — per-turn `EOU_WAIT uuid=... turn=N eou_wait_ms=M text_len=K` (коммит `d2f875d1`). Baseline EOU_WAIT собран на 22 turns (docs/EOU_BASELINE_2026-04-18.md).
- [x] **`yandex_stt_grpc.py` sync в DEV git** — коммит `296505c2`. Устранён scp-only drift pattern (прецедент — тот же что с Observer `49659ffb` 08.04).
- [x] **RIZALTA prompt v4 → v4.1 → v4.2** — три итерации: v4 (`ffcde20e`) added ЧЕЛОВЕЧНОСТЬ / ФАКТЫ ДОСЛОВНО / МОЖНО ВАРЬИРОВАТЬ секции; v4.1 (`ab90940b`) replaced stress-problematic phrases («Дальше интересно?»/«Хочется подробнее?» Alena неправильно ударяла + duplicate «Продолжить?»); v4.2 (`110a6a3c`) natural phrasing («Про условия рассказать?»→«Рассказать про условия?», infinitive variant для offer).
- [x] **Auto-hangup после Sofia farewell (RIZALTA only)** — коммит `b78fe04c`. Hybrid keyword + [END] marker → AS_TYPE_HANGUP 0x00 frame после 1500 мс polite pause с barge-in interlock (cancel если user speaks). Gate `VOICE_PROMPT_MODE=rizalta` (Atlantis не затронут). L0 rollback через `AUTO_HANGUP_ENABLED=false` env.
- [x] **[END] handler fix** — коммит `19102904`. Pre-existing bug: `if "[END]" in answer_text` substring match ДО hallucination filter → LLM emit `[END]` в galluc tail, filter strips tail but `state.dialog_finished` уже flipped. Fix: swap order + anchor `answer_text.rstrip().endswith("[END]")`. Call `f184c7be` был первый проявленный regression; после fix — чисто.
- [x] **Call recording — MixMonitor + 72h→Opus rotation** — коммит `19a49fa6`. MixMonitor в обоих dialplan-контекстах. Cron 03:00/03:30 UTC. Inbound verified `aa568cc1` (792 KB, 49.5s, both sides audible).
- [x] **dial.py realtime transcript streaming** — коммит `4ce5881b`. subprocess(journalctl -f) + daemon thread + регекс matchers на USER/SOFIA/AUTO_HANGUP. Gate `STREAM_TRANSCRIPT=true` default. Time-filtered (start tail on state→Up, stop on Ended).

## Завершено (17.04.2026, вечер)

- [x] **Outbound dialer v1 — `dial.py` + `[outbound-audiosocket]` dialplan context** — `/opt/sofia-voice/dial.py` normalize phone → `channel originate` → CDR polling (uniqueid + timestamp+dcontext fallback) → JSONL в `/var/log/sofia-voice/outbound_YYYY-MM-DD.jsonl` → exit code по disposition. `[outbound-audiosocket]` zerkal [from-telphin] с `Set(CALL_UUID=${SHELL(/proc/sys/kernel/random/uuid)})` → `AudioSocket(${CALL_UUID},127.0.0.1:9090)`. UFW добавлено правило на второй Telphin IP `213.170.84.105:5090/udp`. Живой тест `98332baf` (119s, 7 turns RIZALTA) — нормальный UX. Коммит `96b68ae7`.
- [x] **dial.py fixes follow-up** — два related bug'а обнаружены после 96b68ae7: (1) `find_cdr_fallback` искал по `phone in channel` (для outbound CDR row не содержит номер) → заменено на timestamp+dcontext match; (2) `cdr_line_count` считал byte-level newlines, но CDR CSV с embedded newlines в quoted CallerID полях даёт 256 newlines vs 254 csv rows → замена на `sum(1 for _ in csv.reader(f))`; (3) `captured_uniqueid` добавлен в JSONL для post-mortem сравнения с CDR uniqueid (подтверждает P3 concise parser bug). Коммит `24dfc4b8`.
- [x] **Realtime status events + post-call transcript from SQLite** — dial.py теперь выводит `[HH:MM:SS] Originating → Ringing → Answered → Ended` через 1с polling `core show channels concise` (substring-match, не field-index). После summary — транскрипт из `/opt/sofia-voice/sofia_voice.db messages` table (chat_id derived via md5-formula mirror voice_asterisk.py:125) → файл `/var/log/sofia-voice/transcripts/transcript_<UUID>.txt` + stdout блок `--- Transcript ---` с формой `[MM:SS] SOFIA/USER: text`. JSONL дополнен `transcript_path` и `message_count`. Unit-тест на историческом `dfc4aee9` (7 сообщений) и `86c53f97` (1 сообщение). Коммит `ec410b3b`.
- [x] **YANDEX_STT_V3_INFRA_v1 (Phase 2A)** — grpcio 1.80.0 в venv VPS, `yandex_proto/` v3 скопирован (29 файлов), `/opt/sofia-voice/yandex_stt_grpc.py` создан как callback-based `YandexSTTStream`, smoke-tested с 320B silence (auth + bidirectional stream + clean close). Коммит `f2d8d8df`.
- [x] **YANDEX_STT_V3_INTEGRATION_v1 (Phase 2B+2C)** — Phase 2B: bifurcate через method-pointer `_process_audio` под `STT_MODE=grpc`, `text_override` в `_process_turn`, live-test `fe7eed5c` (длинные фразы с микропаузами склеены целиком). Phase 2C: continuous streaming + TTS-echo guard в `_on_grpc_*` через `_tts_playing and not _cancel_playback`, live-test `4b109925` (8 turns / 3 barge-in / 0 false turns / phone AEC работает). Коммит `e13aa3fa`.
- [x] **YANDEX_STT_V3_EOU_TUNING (Phase 2D)** — env-switch `YANDEX_STT_EOU_MODE=default|high` в `yandex_stt_grpc.py` + `voice_asterisk.py`. Live-test `4ad65942` подтвердил: EOU wait −320мс медиана / −620мс на коротких фразах, user pause −980мс на коротких, длинные фразы с микропаузами (100+ симв) НЕ порезаны. Prod `.env`: `STT_MODE=grpc, YANDEX_STT_EOU_MODE=high`. Rollback — одной env-строкой, без кода. Коммит `9114d825`.

## Завершено (17.04.2026, утро)

- [x] **FAIL2BAN_DEPLOY_v1** — установка fail2ban на VPS sofia-voice, два jail (sshd+asterisk), banaction=ufw, 9 SSH-сканеров забанены с первого старта. UFW + fail2ban = двухслойная защита. `ignoreip` включает 46.229.221.93 (Telfin), 72.56.64.91 (главный сервер), 77.106.252.172 (личный Сергея). `[asterisk]` — явный `backend=auto` на `messages.log` (journal у asterisk пустой). Детали и уроки — SESSION_LOG 17.04 (утро) + CLAUDE.md «Уроки из ошибок».

## Завершено (15.04.2026)

- [x] **DISK_RESCUE_v2** — VPS 185.207.66.201 диск 100% → 15%, корневая причина (сломанный с 2022 logrotate-конфиг) найдена и починена. Освобождено 25 GB. Asterisk + sofia-voice не пострадали, mtime configs не изменился. Бэкап старого конфига в `/etc/logrotate.d/asterisk.broken.20260415`
- [x] **CONTEXT_RECONCILIATION_v1** — сверка модели Claude Code с реальностью по 7 пунктам. Sprint 2 verified в production (лид 265712, @pordiregrwe → UF_CRM_TELEGRAM заполнен, БП 152 отработал)
- [x] **ORCHESTRATOR_RULES.md** — отдельный документ с правилами работы оркестратора, зафиксированы уроки 10-15.04
- [x] **logrotate для Asterisk** — починен корневой баг конфига (был в P1 как "настроить logrotate")
- [x] **UPDATE_GITPULL_DOCS** — блок «Gitpull» в CLAUDE.md дополнен 4-й scp-командой для `docs/ORCHESTRATOR_RULES.md` + `mkdir docs/`. Блок «Session End» обновлён. Локальный `start-session.sh` Сергей обновил синхронно. Коммит 5fee930c.
- [x] **ATLANTIS_CHAT_WIDGET_v1** — `widgets/atlantis_chat_widget.html` (15.9 KB), самодостаточный HTML-блок для Tilda T123. Десктоп: круг 72×72 с pulse-ring. Мобильный: плашка снизу с аватаркой. Ведёт в @SofiaOazis через вторую Telegram Business Link. Круг зелёный по фидбеку Сергея. Коммит 3de1fbf9.
- [x] **SPRINT_TEMPLATE_v2** — `docs/SPRINT_TEMPLATE_v2.md` (13.3 KB) на XML-структуре по гайду Anthropic. 9 обязательных секций + 4 опциональных + 2 встроенных блока (anti_overengineering, investigate_before_answering) + filling example. Коммит 057b6811.
- [x] **TEMPLATE_V2_WIRING** — шаблон проведён в ORCHESTRATOR_RULES.md (правило «новые контракты строить по SPRINT_TEMPLATE_v2.md») и в Gitpull блок CLAUDE.md. Коммит 71103849.
- [x] **SAFETY_RULES_v1 (DEV)** — блок «ЧЕГО SOFIA НЕ ДЕЛАЕТ» в `sofia_prompt_v2.py` (универсальные запреты: паспорт/реквизиты/коды/гарантии/доходность/бронь) и блок «ПЕРЕДАЁТ ДИАЛОГ МЕНЕДЖЕРУ» в Atlantis-addon (политика/юр-фин компании/СВО/дискриминация/незаконные схемы/мошенничество). DEV smoke #2 подтвердил работу общего ядра. Коммит 772cbe6e `--no-verify` (pre-existing долг в format_state_summary).
- [x] **SAFETY_RULES_DEPLOY_PROD (split, core only)** — портировано в PROD **только** `sofia_prompt_v2.py` (блок «ЧЕГО SOFIA НЕ ДЕЛАЕТ», работает во всех трёх PROD-каналах). Atlantis-addon часть отложена до ATLANTIS_ADDON_INFRA_PORTBACK (в PROD отсутствует механизм чтения prompt_addon целиком). PROD smoke с СМС-триггером подтвердил работу. Все три сервиса active, 0 ошибок за 5-мин мониторинг. PROD коммит d60e9b0b, без push.
- [x] **GAPS_CLOSE_BEFORE_NEXT_SESSION** — закрыты 3 пробела перед следующей сессией: (1) стилистические правила Сергея («lf»=да, запрет ритуальных оборотов, короткое признание ошибок, один шаг=одно сообщение, «человеческим языком») добавлены пунктами 6-10 в ORCHESTRATOR_RULES.md «Стиль ответов»; (2) 4 критичных memory-файла добавлены в Gitpull блок CLAUDE.md (project_prod_dev_drift, user_sergey, project_voice_sprint_next, project_voice_vps); (3) в P3 заведён пункт на обновление userMemories оркестратора. Коммит 3081ea0e.

## Завершено (11.04.2026)

- [x] **Деплой Radist username→Bitrix в PROD** — sofia_radist_gateway.py + core/bitrix.py скопированы в PROD, sofia-radist.service перезапущен, логи чистые. PROD коммит 1146ea2c. (11.04)

## Завершено (10.04.2026)

- [x] **Аудит Атлантиса** — docs/AUDIT_ATLANTIS_2026-04-10.md, 8 секций, зафиксирован переход на @SofiaOazis, 5 открытых проблем с приоритетами
- [x] **Спринт 1: merge bot_server.py** — Observer portback из PROD в DEV (6 блоков, 86 строк), единственное различие осталось — env-default OBSERVER_CHAT_ID. DEV стал deploy-ready. Коммит 75b779a5.
- [x] **Спринт 2: Radist username → Bitrix** — chat.username из webhook → UF_CRM_TELEGRAM + COMMENTS. DEV коммит 8b504869, PROD деплой 11.04 коммит 1146ea2c.
- [x] **Фаза 4: systemd + health check** — sofia-voice.service на VPS, Restart=on-failure, auto-restart протестирован. Голосовой канал защищён от ребутов.
- [x] **TTS A/B тест: v3 locked** — live A/B: v3 > MLv2 > Flash v2.5. Eleven v3 = production TTS (10.04)
- [x] **Stress Research** — ruaccent 75%/388ms отклонён, U+0301 не документирован → ручной словарь (10.04)
- [x] **ElevenLabs v3 Research** — model_id, streaming, TTFB, PVC compatibility, pricing (10.04)
- [x] **Sync DEV от VPS** — voice_asterisk.py + core/pipeline.py синхронизированы, коммиты 8d468477 + 04d9a710 (10.04)

## Завершено (09.04.2026)

- [x] **Radist dedup fix** — save_message+notify_observer вынесены из цикла queue, одна запись на пачку сообщений (DEV+PROD)
- [x] **Voice Research Sprint** — 54 аудиосемпла, 9 LLM, 16 TTS, 7 STT, отчёт RESEARCH_REPORT_VOICE_AGENT_v1.md
- [x] **Voice Stack Tuning v2** — kimi-k2 LLM, Flash v2.5 → v3 TTS, новые VoiceSettings, VAD 300ms, SSML breaks
- [x] **RIZALTA Prompt v2** — канонический питч, «всегда вопрос», Софья→София, 6 примеров диалогов
- [x] **Groq платный план** — решено через kimi-k2 (throttling не наблюдается)

## Завершено (28.03–06.04.2026)

- [x] **Фикс SOFIA_PATH в prod .env** — prod бот писал в dev БД, повтор бага 21.03 (05.04)
- [x] **Фикс логирования bitrix.py** — logging.basicConfig в bot_server.py, логи [BITRIX] видны в journalctl (05.04)
- [x] **Диагностика лидов 264270/264264/264260/264252** — все распределены, клиенты не заходили в бот (05.04)
- [x] **Деплой Bitrix-интеграции в PROD** — finalize_lead, start_distribution_bp, дедупликация по телефону (04.04)
- [x] **Фикс ASSIGNED_BY_ID=24932 перед БП** — БП 152 требует конкретного ответственного (04.04)
- [x] **Фикс manager_active в state_manager** — краш таймаута "no such column" (04.04)
- [x] **Новый webhook-токен с bizproc** — uviwr0b350u3m6gf (04.04)
- [x] **Фикс redirect Тильды** — был dev-бот, заменён на prod (04.04)
- [x] **E2E тест БП раздачи** — форма → лид → TG → 15 мин → BP 152 → раздача (04.04)
- [x] **docs/LEAD_LIFECYCLE.md** — архитектура CRM-интеграции (04.04)
- [x] **docs/NEW_CLIENT_ONBOARDING.md** — инструкция подключения нового ЖК (04.04)
- [x] **Голосовой стек Телфин → Asterisk → AudioSocket → STT/LLM/TTS** — полный pipeline на VPS (06.04)
- [x] **Asterisk + SIP trunk Телфин** — PJSIP, порт 5090, Registered (06.04)
- [x] **AudioSocket сервер** — voice_asterisk.py, TCP протокол, двусторонний аудио (06.04)
- [x] **Nginx LLM proxy** — 72.56.64.91:8095 для Groq/OpenAI/ElevenLabs из России (06.04)
- [x] **TTS streaming ElevenLabs** — first audio ~400-500ms вместо 5-8 сек (06.04)
- [x] **VAD 0.7s + barge-in** — перебивание останавливает TTS, контекст сохраняется (06.04)
- [x] **Фикс промпта "Повторите" на "Завтра"** — добавлен пример для дат/времени (06.04)
- [x] **A1: VAD threshold 0.4s** — снижено с 0.7s, -300ms ощущаемой паузы (07.04)
- [x] **ElevenLabs voice tuning** — stability=0.35, similarity=0.79, speed=1.19 (07.04)
- [x] **Research Qwen3-TTS** — 30 аудиофайлов, WaveSpeed API, отчёт готов (07.04)
- [x] **TASK_DUMP_SOFIA_PROMPTS.md** — полный дамп всех 9 промптов системы (07.04)
- [x] **Bitrix в Radist gateway** — radist_leads, ATL-привязка, таймауты 15/60/15, manager_active (08.04)
- [x] **Окно матчинга 60 сек** — find_recent_atlantis_lead фильтр >=DATE_CREATE (08.04)
- [x] **RIZALTA промпт v1** — VOICE_SYSTEM_PROMPT_RIZALTA + VOICE_PROMPT_MODE switch (08.04)
- [x] **Git recovery после краша** — DEV 3 коммита, PROD 5 коммитов по каналам (08.04)
- [x] **ElevenLabs multilingual_v2** — switch для PVC голоса Nastya (08.04)
- [x] Промпт V5-V5.2: persona, few-shot, позитивные инструкции (b34e5ffc → 8e0a7738)
- [x] Barge-in pending: замена debounce, склейка фраз (f83631ae)
- [x] meeting_agreed context check (e9b47719)
- [x] Voice pipeline v4: Groq llama-4-scout primary (TTFT 99ms)
- [x] Битрикс: БП раздачи (template 152) — finalize_lead() (c1987ff5)
- [x] Дедупликация транскриптов Retell (3-уровневая)
- [x] Budget парсинг (дефис, "от-до", числительные)
- [x] Исследование LLM: Groq, YandexGPT, Fireworks, Together, DeepSeek
