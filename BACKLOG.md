# BACKLOG — Невыполненные задачи

## P0 — Срочно

- [x] ~~**VOICE_YANDEX_MIGRATION_v1**~~ — выполнено 16.04: LLM→YandexGPT 5 Pro, TTS→Yandex Alena, cached greeting (0ms TTFB), anti-hallucination filter. A/B 4 моделей — Pro победил (LLM 638ms, user pause 860ms). TTS TTFB 177ms (было 1100ms). Dead branches сохранены для rollback.
- [x] ~~**RIZALTA промпт v3 + barge-in fix**~~ — выполнено 16.04: двухчастный питч без представления, логика мессенджеров (Ватсап/Макс/Телеграм), вход 15 млн, Совкомбанк/Сбер ипотека. Cached greeting регенерирован. Barge-in cooldown 500ms + MIN_SPEECH_BYTES 10240 задеплоены. Живой звонок UUID 21b96626 подтвердил: cooldown сработал 2×, MIN_BYTES 3×, логика мессенджеров дошла. Коммит `ef2888ae`.
- [x] ~~**🔴 Barge-in buffer reset вместо re-feed**~~ — выполнено 17.04 (коммит `5104a0cc`). Звонок 21b96626 (16.04 вечер) подтвердил: 13KB re-fed buffer → garbled STT → «плохо слышно» cascade. Fix: `.clear()` без re-feed в `_process_turn` finally.
- [x] ~~**🔴 Silero VAD замена energy-based RMS**~~ — выполнено 17.04 (4 коммита): `761ef1eb` Silero v4 integration (v5 дал 13% detection на 8kHz, v4 73%), `97934f47` sliding-silence reset (chunking mismatch 320B frames vs 512B chunks), `830a5722` `_tts_playing` флаг (pre-TTS race), `95b4eeae` VAD_SILENCE 300→500ms + barge-in confirm 10 frames Silero / 5 energy. Финальный звонок 95e12e88 подтвердил: 8 turns, 0 deadlocks, полный продажный flow с Telegram-ником.
- [x] ~~**🔴 fail2ban/UFW на VPS 185.207.66.201**~~ — **UFW выполнено 17.04**, **fail2ban выполнено 17.04 (утро)** (см. «Завершено 17.04.2026, утро»). Двухслойная защита активна.
- [ ] **ATLANTIS_ADDON_INFRA_PORTBACK** — в PROD отсутствует механизм чтения `prompt_addon`: в `config/source_objects.py` Атлантис-объект закрывается сразу после `greeting` (ключа `prompt_addon` нет), в `core/pipeline.py` нет ветки `if obj_config.get("prompt_addon")`. Весь Atlantis-addon-механизм — DEV-only фича, никогда не был в PROD. Sofia в PROD Atlantis-flow не получает специальных правил, включая давнее «не запрашивай контакты, клиент уже из формы» и новые safety-триггеры (СВО/политика/дискриминация/мошенничество). 400+ строк разницы между PROD и DEV `core/pipeline.py`. Требуется отдельный спринт: полный diff pipeline.py PROD vs DEV, классификация расхождений, план порта по частям, контролируемый деплой с разведкой. Блокер для работы всех Atlantis-specific правил в production.
- [ ] **Git-sanity scan prod vs dev** — пройтись по всем логическим файлам (core/, config/, *_server.py, *_gateway.py), сравнить md5 HEAD в prod и dev, найти расхождения. Уже известно: `core/pipeline.py` (400+ строк разницы, см. ATLANTIS_ADDON_INFRA_PORTBACK), `config/source_objects.py` (black-drift на `detect_source`/`load_object_context` + отсутствие `prompt_addon`). Могут быть другие. Мини-спринт на 15-20 мин после ATLANTIS_ADDON_INFRA_PORTBACK.
- [x] ~~**B1: Filler звуки**~~ — закрыто 16.04: не актуально после миграции на Yandex. TTS TTFB 177ms (было 700-900ms ElevenLabs v3), fillers не нужны при такой латентности.
- [x] ~~**A3: Sentence-level TTS streaming**~~ — закрыто 16.04: не актуально. Yandex TTS v1 REST TTFB 177ms быстрее чем ElevenLabs streaming был. Gain минимален.
- [x] ~~**🔴 Апгрейд VPS sofia-voice до 2-4 vCPU**~~ — **выполнено 20.04** (Timeweb Premium NVMe, 1→2 vCPU, 4 GB RAM). nproc=2, load 0.00, RAM 3.2 GB free. **Гипотеза не подтвердилась:** проглатывания слогов сохранились после апгрейда на silent test. 1 vCPU не был bottleneck. Настоящий виновник — AudioSocket buffer overflow из-за pacing 18ms vs 20ms playback (см. VOICE_PACING_FIX ниже).
- [x] ~~**🔴 VOICE_PACING_FIX_v1**~~ — **выполнено 20.04** (коммиты `8f3eeaf0` + `7f65fadd`). Локализовано через silent test `b3cd88c8` + byte-level сравнение greeting_source vs MixMonitor (correlation 0.034 → 0.987 после фикса). Асимметричный фикс: `_speak_pcm=0.020` (без параллельной нагрузки), `_speak=0.019` (компенсирует ~855us event loop overhead от gRPC+Silero). Per-chunk wall ≈ 20.0ms ровно. 0 dropouts в TTS-репликах. Подтверждено живым звонком Сергея `dff69ac4` (126с, 7 реплик, 3395 чанков). PROD не тронут (voice-компонентов в `/opt/sofia-gpt/` нет).
- [ ] **🔴 «Склейки внутри слов» диагностика** — **новое P0 на 20.04**. Pacing-фикс убрал проглатывания, но на стыках слогов в TTS осталась шероховатость (artefacts waveform phase, не dropouts). Бесплатный первый шаг: Сергей слушает уже скачанные A/B файлы на Mac (`call_8ad55f5b_reply5_{baseline,48k,jane}.wav` + reply8). Три возможных исхода: (1) 48k чище → Sprint на inline 48kHz→soxr в `synthesize_tts_yandex` (+150мс latency, ~20 строк), (2) все одинаковые → физика 8kHz PSTN/G.711, accept as-is, (3) jane чище → `YANDEX_TTS_VOICE=jane` env-переключатель. Дополнительно проверить `speed=1.0` vs `1.1`. Эффорт диагностика: 5 мин прослушивания Сергеем; фикс (если 48k): 2 часа.
- [ ] **Ударения Yandex Alena** — проверить ударения на ключевых словах (Белокуриха, РИЗАЛТА, эскроу). Yandex SpeechKit лучше ElevenLabs на русском (~99% vs ~80%), но нужна проверка.
- [ ] **Proxy monitoring** — алерт если 8095 упал (теперь менее критично — голос не использует proxy, только текстовые каналы)

## P1 — Следующая итерация

- [x] ~~**🔴 SSH alias sofia-voice починить**~~ — закрыто 18.04 утро. Алиас работает корректно на протяжении всей сессии (12 коммитов, десятки команд `ssh sofia-voice`).
- [x] ~~**🔴 Запись звонков (MixMonitor WAV)**~~ — **выполнено 18.04** (коммит `19a49fa6`). MixMonitor в обоих dialplan-контекстах (`[from-telphin]` + `[outbound-audiosocket]`), `/var/lib/asterisk/recordings/YYYY-MM-DD/<UUID>.wav`, cron 03:00 конвертация WAV>72ч в Opus 32kbps + 03:30 retention 30д. Rotate скрипт `bin/rotate_recordings.sh` идемпотентен, atomic (WAV удаляется только после ffmpeg rc=0). Inbound verified `aa568cc1` (792 KB/49.5с/обе стороны audible).
- [x] ~~**🔴 sofia-voice.service ExecStop=hangup_audiosocket.sh — защита от orphan AudioSocket**~~ — **выполнено 21.04**, коммит `37e099a5`. `bin/hangup_audiosocket.sh` (awk `$6=="AudioSocket"` → targeted hangup) + `ExecStop=...` в unit. Smoke: restart во время активного PJSIP-канала → через 3с `core show channels count=0`, syslog `sofia-voice-hangup: hangup PJSIP/...`, auto-restart через `Restart=on-failure` (MainPID 24230→42525). Part 2 regression (нормальный workflow, graceful hangup клиентом) — ExecStop не вызван, disposition=ANSWERED billsec=43. Snapshot unit `/tmp/sofia-voice.service.before_execstop_20260421_090953`.
- [ ] **🔴 Cron load_watchdog на VPS — safety net** — `/etc/cron.d/sofia-voice-watchdog` + `/opt/sofia-voice/bin/load_watchdog.sh`. Запуск каждые 2 мин. Триггер: `load1 > 15 AND asterisk_cpu > 150%` (оба вместе, не OR — легитимные пики не должны срабатывать). Debounce 10 мин через `/tmp/watchdog.last`. Действие: `systemctl restart asterisk` + лог в `/var/log/sofia-voice/watchdog.log`. Telegram alert через curl на Bot API (опционально — `TELEGRAM_BOT_TOKEN` + `ADMIN_CHAT_ID` сейчас отсутствуют в VPS `.env`, нужно добавить из main server `/opt/sofia-gpt/.env` либо оставить лог-only). Защита от воспроизведения инцидента 20.04 без участия человека. Полный draft скрипта в Build Report ORPHAN_CLEANUP_RECON_v1. **Отложено на P1 решением Сергея 20.04: возвращаемся после первых пилотных звонков с данными о реальном load pattern'е.** Эффорт: 30 мин.
- [ ] **🟡 Greeting PCM playback artefacts diagnostic** — **новое P1 на 20.04 (session 2)**. Speed A/B эксперимент (1.1→1.0→1.2) показал: **speed=1.2 даёт субъективное улучшение на live-речи, но НЕ на cached greeting** (`greeting_rizalta.pcm`). Артефакты на стыках слогов в greeting остаются стабильными на всех трёх значениях speed. Это при том, что транскодинг пути идентичны: оба идут через `_speak_pcm` → AudioSocket (slin 320B/20ms) → Asterisk G.711 alaw → Telphin. SIP codec recon (Sprint F, 20.04) исключил codec-level причину — Telphin offerит только G.711 alaw/ulaw, G.722 недоступен, переключение alaw↔ulaw косметика. Гипотезы: (1) static file readback artefacts vs live streaming state, (2) детерминированные silence gaps между `add_ssml_breaks` sentences в cached отличаются от адаптивных в live, (3) артефакты waveform phase при 8kHz decimation из сохранённого PCM. **Бесплатный первый шаг:** выключить cached-load на 1 тестовый звонок (live-синтез того же greeting-текста), записать через MixMonitor, сравнить с MixMonitor'ом cached-звонка. Если live чистый → причина в cached-path. Если оба грязные → физика 8kHz/G.711 PSTN. Эффорт диагностики: 30 мин (feature flag + 2 звонка + WAV diff). Фикс (если cached-path): зависит от root cause. Не связан с codec negotiation — тот тупик документирован. **`YANDEX_TTS_SPEED=1.2` зафиксирован** в .env VPS как оптимальное значение.
- [ ] **🔴 Пилотная живая серия RIZALTA inbound — перезапуск после pacing-фикса 20.04** — 18.04 вечер: первые 3 звонка от тестера +79160322905 пришли (UUIDs `e561a7c6`, `94ea75b1`, `b2786722`). 19.04 дополнительной активности не было — 24ч тишины. 20.04 фикс проглатывания задеплоен. Следующий шаг: Сергей делает 2-3 собственных живых звонка для финальной ear-validation фикса. После — перезапускаем пилот: зовём тестеров (5-6 × 5-6 звонков), собираем 20+ записей с чистым pacing, cross-ref transcripts / dropouts / false refusals. Если всё ок — RIZALTA готов к реальному обзвону. Блокер на подтверждение «Склейки внутри слов» — если критично, пилот после inline-48kHz спринта.
- [ ] **🔴 Text-path [END] handler в PROD `core/pipeline.py:319-326`** — тот же pre-existing bug что мы fix'ed в голосе коммитом `19102904` (`"[END]" in answer` substring match ДО anti-hallucination filter). Применить тот же swap+anchor паттерн в text-path. В PROD репо `/opt/sofia-gpt/` — влияет на Telegram/Radist/web Atlantis inbound. Критично: при галлюцинации LLM false `dialog_finished` → Sofia перестаёт отвечать в середине разговора. Эффорт: ~1 час (swap order + anchor + smoke test).
- [ ] **Option A RIZALTA prompt — двойное подтверждение отказа** — из NOISE_CASE_POSTMORTEM. Добавить в Категорический отказ branch требование двух refusal-signals (one explicit lexeme — «нет»/«не интересно»/«не надо» — AND повторный refusal OR sustained «повторите» confusion). На ambiguous user input defaulter к «повторите», НЕ к refusal. Reuse Atlantis pattern («дважды чётко отказался»). Эффорт: ~2 часа + regression tests. Запускать если 19.04 пилот покажет повторные false refusals (как на call `f8d41815`).
- [x] ~~**Audio dropouts diagnostic**~~ — **выполнено 20.04**. Корневая причина — AudioSocket buffer overflow из-за pacing 18ms vs 20ms playback. Не CPU contention (апгрейд до 2 vCPU не помог). Не TTS (curl-A/B исключил). Чистый overflow → Asterisk дропает лишние фреймы (209 dropout-окон в 12с греeting, correlation source vs MixMon 0.034). Fix: pacing `_speak=0.019 / _speak_pcm=0.020`. См. SESSION_LOG 20.04 полная хроника.
- [ ] **Post-barge-in ack timer** — **отложено до сбора обратной связи** от живых RIZALTA-звонков. Pre-existing deadlock-класс при коротких barge-in: клиент перебивает коротко (~200мс), ожидает ack от Sofia, Sofia молчит (нет EOU триггера) → 7+ секунд тишины пока клиент не скажет «алло». Подтверждено в REST `71b461f4` (8.36с), gRPC `4b109925` BI#2 (7.3с), gRPC EOU=high `4ad65942` BI#3 (9.3с). Решение (когда возьмёмся): в `_process_turn` finally при exit через barge-in — `asyncio.create_task` с `asyncio.sleep(3)` → если за 3с новый turn не начался → Sofia говорит «я вас слушаю?». Пер-режимно независимо (STT_MODE=rest/grpc). Не берём сейчас — перед тем как оптимизировать UX, нужны данные: как часто такой deadlock случается на реальных RIZALTA-звонках (не тестовых).
- [x] ~~**`_speak` per-chunk instrumentation**~~ — **выполнено 20.04**. Инструментация добавлена на ~8 часов, собрано 8600+ CHUNK-строк с 4 звонков, снята в session-end коммите `8f3eeaf0`. Дала финальные цифры для pacing-фикса (sleep_overrun median/p95/max, inter-chunk gap distribution, per-reply wall vs audio excess). Шаблон инструментации сохранён в snapshot `/tmp/voice_asterisk.py.before_cleanup_20260420_071727` на VPS для будущих диагностик.
- [ ] **🟡 dial.py misleading "FAILED (no CDR after 600s)" message** — косметика (P1 с 21.04). При channel_giveup-exit (35с) stdout выводит «after 600s» т.к. `main()` пишет единый текст для обеих no-CDR веток. Пробросить reason наверх из `wait_for_cdr` (timeout vs channel_never_appeared) + разные message'и в main. ~15 мин. Не блокер — exit-code и jsonl корректны, только вывод в терминал путает.
- [ ] **Silero VAD barge-in false positives на TTS-echo** — на звонке `94ea75b1` (18.04) reply #2 обрезана через 497мс после старта TTS: Silero набрал 200мс (3200 bytes) cumulative при `SILERO_VAD_THRESHOLD_BARGE_IN=0.5` на SIP линии с TTS-echo. Комментарий в коде прямо: «same as normal: TTS-echo mix on SIP line». Клиент реальный вопрос задал через 4.7с (т.е. VAD принял за речь не речь). Варианты фикса: (a) threshold 0.6-0.7 на TTS-период, (b) удлинить cumulative window 200→400мс (BARGE_IN_SILENCE_RESET), (c) обе. Не делать до сбора статистики на большем числе звонков после пилота (хотим понять частоту false-positive). Эффорт: 30 мин правка + 1 день наблюдений.
- [ ] **Structured turn_metrics JSONL** — после MixMonitor (выше) добавить в `voice_asterisk.py` функцию `log_turn_metric(event_type, data)` пишущую в `/var/log/sofia-voice/turn_metrics_YYYY-MM-DD.jsonl`. События: `turn_start, stt_done, llm_done, tts_first_chunk, tts_done, barge_in, deadlock_detected`. Поля: `{call_uuid, turn_idx, user_text, asst_text, eou_wait_ms, llm_ms, tts_first_ms, barge_in_count, post_bi_silence_ms, deadlock_flag}`. logrotate daily rotate 30. По метрикам оценить частоту deadlock-класса и приоритизировать ack-timer vs другие оптимизации. Эффорт: 0.5 дня. Связано с P2 «Per-turn JSON metrics baseline» — объединить.
- [x] ~~**Outbound dialing v1** — dial.py + [outbound-audiosocket] dialplan context~~ — выполнено 17.04 вечер. Скрипт `/opt/sofia-voice/dial.py`: normalize phone → `channel originate PJSIP/+7...@telphin-endpoint extension s@outbound-audiosocket` → capture uniqueid → CDR polling (primary: uniqueid, fallback: timestamp+dcontext) → JSONL в `/var/log/sofia-voice/outbound_YYYY-MM-DD.jsonl`. Протестирован на +79181011091 (звонок `98332baf`, 119с, нормальный UX, 7 turns RIZALTA). UFW добавлено правило на второй Telphin IP `213.170.84.105:5090/udp`. Коммит Outbound v1 с fallback-fix.
- [ ] **Outbound dialing v2 — batch + retry + CRM** — следующий уровень поверх v1: чтение списка номеров из файла/Bitrix, rate limit, retry на недозвоны, callback schedule, финализация лида в Bitrix. Текущий v1 — один звонок на вызов скрипта, без state. Эффорт: 2-3 дня. **Бизнес-зависимость:** готовый список для прозвона + legal constraints.
- [ ] **Sentence-level LLM streaming → TTS pipelining** — YandexGPT 5 Pro поддерживает `stream=True` SSE. Код сейчас ждёт `full_text = resp.choices[0].message.content` полностью. Переписать на async iterator: как только готово первое предложение — в TTS streaming, начинать играть, параллельно синтезировать второе. Нужен sentence splitter стойкий к «10 млн.» и anti-hallucination stop-sequence на partial. Gain: −200-350ms медиана на репликах >1 предложения. Эффорт: 2 дня. Идея #3.
- [ ] **TTS cache canonical phrases + backchannel fillers** — расширить механизм `greeting_rizalta.pcm` на весь каноничный питч и типовые ответы («Понимаю», «Хорошего дня», «Простите, плохо слышно»). Плюс 5-10 pre-rendered filler-PCM («Так…», «Секундочку») с intent-classifier. Гейт: проигрывать filler при ожидаемом LLM >500ms. Gain: cache hit → 5ms TTS, перцептивная пауза → <100ms. Эффорт: 1.5 дня. Идеи #6+#7.
- [ ] **Sprint 2: Sofia-GPT текстовый стек миграция на Yandex** — Extractor+Analyzer→YandexGPT Lite (дёшево, быстро для NLU), Generator→Qwen3-235B или Alice AI LLM (качество важнее скорости для текста), Embeddings→Yandex. A/B через promptfoo. Мотивация: после голосовой миграции 16.04 текстовый стек остался на OpenAI (gpt-5.2) — единая платформа снижает зависимости и стоимость.
- [ ] **Рефакторинг find_recent_atlantis_lead()** → универсальный `find_recent_lead_by_source(source_id)` — сейчас хардкод на SOURCE_ID=397, блокер для масштабирования на новые ЖК
- [ ] **Вынести хардкоды в env/config** — SOFIA_AI_USER_ID (428), BITRIX_ATLANTIS_SOURCE_ID (397) → в .env или source_objects.py
- [ ] **Спринт 3: возврат виджета в новом формате** — slide-out side tab на invest-apartmens.online/atlantis (заменяет старый круглый bubble). Требует: новая вёрстка (side tab вместо bubble, брендинг Атлантиса сохранить), URL-based source_object detection (по page_url а не по ключевым словам в тексте), разделение prompt_addon по каналам: Telegram — не спрашивать контакты, виджет — обязательно спрашивать телефон, таймаут финализации для виджет-сессий (или покрытие Bitrix-роботом), Bitrix-интеграция: source_id, attention к ASSIGNED=428 flow. **БЛОКЕР:** ждёт verification что 20-минутный Bitrix-робот реально работает — без safety-net виджет утопит CRM зависшими лидами.
- [ ] **Safety-net verification** — после того как админ добьёт 20-минутный робот в Bitrix: проверить что робот ловит Тильда-лидов с ASSIGNED=428 старше 20 мин и корректно передаёт менеджеру через БП 152. Тест-кейсы: лид #264518 (Роман) и аналогичные. Если робот не покрывает — план Б: cron внутри Sofia.
- [ ] **source_objects.py prompt_addon разделение по каналам** — сейчас DEV имеет незакоммиченный prompt_addon "не запрашивай контакты", который применяется ко всем Атлантис-сессиям включая виджет (где логика должна быть обратной). Нужно разделение: Telegram наследует prompt_addon, виджет — нет. Связано со Спринтом 3.
- [ ] **Таймаут для web-виджета** — сейчас web-сессии не финализируются по таймауту, только по [END] от LLM
- [ ] **Лиды без бот-диалога** — клиенты из Тильды, не перешедшие в TG, не проходят квалификацию Софией. Варианты: CRM-робот с таймаутом или принять как нормальный путь
- [x] ~~**Битрикс в radist_gateway.py**~~ — выполнено 08.04: radist_leads таблица, привязка ATL-лидов, таймауты 15/60/15
- [ ] **Groq платный план** — бесплатный throttled 70% запросов в A/B тесте
- [ ] **Retell begin_message** — убрать или настроить тот же голос что в ответах
- [ ] **Retell Transcription Mode** — accuracy vs speed тест
- [ ] **Post-Call Data Extraction** — goal, budget, payment_type, meeting_agreed
- [ ] **deploy.sh** — скрипт деплоя (сейчас ручной cp + restart)

## P2 — Улучшения Voice

- [ ] **Telphin autodial migration — ждём ответ менеджера** — новое P2 21.04. У Телфина есть «Голосовой робот» (1790₽/мес, до 100 одновременных, predictive-mode) и «Услуга активного автонабора», но в public docs не упомянуты AMD (Answer Machine Detection) и passthrough живого ответа на внешний SIP endpoint. Сергей написал менеджеру 4 вопроса: (1) поддерживается ли AMD, (2) можно ли «dial list → при живом ответе передать на наш AudioSocket 185.207.66.201», (3) возвращает ли `answer` webhook маркер human vs machine, (4) media forwarding интерфейс. **Если да** → свой `dial.py` устаревает, инфраструктура Телфина берёт фильтрацию недозвонов → переход на ~1 день работы. **Если нет** → продолжаем текущий стек, пилот уже рабочий. Блокер: ответ менеджера. Разведка в SESSION_LOG 21.04.

- [ ] **VPS `/opt/sofia-voice/` не git-tracked — workflow риск** — обнаружено 20.04 при session-close: VPS sofia-voice не имеет `.git`, правки напрямую живут без истории до тех пор, пока кто-то не синхронизирует в `/opt/sofia-gpt-dev/` на main-сервере (git origin). Риск: следующий DEV→VPS scp деплой может молча перезаписать live-правку. Сегодня финальное состояние синхронизировано в `8f3eeaf0`, но правило явно не зафиксировано. Два варианта решения: (a) правило «любая правка voice_asterisk.py ПЕРВЫМ ДЕЛОМ в DEV, затем scp на VPS» + добавить в CLAUDE.md; (b) `git init` на VPS, `git remote add origin` к main-серверу, pull/push на каждую правку. Вариант (a) проще, вариант (b) надёжнее. Эффорт (a): 15 мин + коммит; (b): 1 час + обучение workflow. Связано с паттерном «prod↔dev drift discipline» из CLAUDE.md.
- [ ] **Bitrix webhook ONCRMLEADUPDATE подписка — defense in depth к stage-guard** — после 19.04 stage-guard через pull-проверку STATUS_ID работает как основной механизм. Webhook-hook в `web_api.py:605 @app.post("/api/bitrix/webhook")` остаётся in-place и полностью живой (HTTPS 200 снаружи, nginx проксирует, find_user_id_by_lead находит Radist-лиды корректно). Stetsenko нужно настроить на стороне Bitrix исходящую подписку `event.bind` для `ONCRMLEADUPDATE` на URL `https://api.atlantis-invest.ru/api/bitrix/webhook`. После настройки — defense in depth: `manager_active` ставится сразу по событию, не ждёт следующего reminder-таймера который вызовет pull-проверку.
- [ ] **Hard-coded `BITRIX_BASE_URL` fallback в `core/bitrix.py:25-28`** — fallback URL с устаревшим токеном `z61dwivdmdy9dk4f`. При живом сервисе безопасно (load_dotenv внутри Python), но любой ad-hoc `python3 -c` без явного dotenv молча получит невалидный токен → все вызовы пойдут в fail-safe path (None/False). Рекомендация: убрать fallback (`os.environ["BITRIX_BASE_URL"]` → KeyError при отсутствии) или добавить startup-проверку через эхо-вызов. Обнаружено при smoke-тесте PROD-деплоя stage-guard.
- [ ] **systemd `EnvironmentFile=` для PROD-сервисов** — `/etc/systemd/system/sofia-{gpt,web-api,radist}.service` не имеют `EnvironmentFile=/opt/sofia-gpt/.env`. Env грузится через `load_dotenv()` внутри Python-кода (WorkingDirectory). Работает, но менее прозрачно: `systemctl show sofia-web-api.service -p Environment` не покажет актуальные ключи. Добавление `EnvironmentFile=` сделает env видимой на системном уровне без смены логики кода.
- [x] ~~**🔴 Апгрейд VPS sofia-voice до 2-4 vCPU**~~ — **поднято в P0 19.04**, см. выше. После эксперимента MIXMONITOR_DISABLE_TEST + A/B TTS матрицы 1 vCPU стал не просто «узким местом на будущее», а главным подозреваемым в реально наблюдаемых проглатываниях слогов.
- [ ] **Dashboard для прослушивания recordings через браузер** — сейчас записи доступны только через scp на локалку или ssh на VPS. Для daily review тестеров / аудита hundreds of WAV нужен web-UI: список по датам → клик → HTML5 audio player + inline transcript из SQLite + журнальные события (AUTO_HANGUP, EOU_WAIT). Стек: простой Flask/FastAPI на VPS port 8092 за nginx/basic-auth. Эффорт: 1-1.5 дня.
- [ ] **Phantom EOU log silencing** — 54 % EOU_WAIT событий в логах имеют `eou_wait_ms=-1, text_len=0` (Yandex duplicate eou_update after every real one). Not a bug, но log noise при analysis — half of events need filtering. Один-LOC guard в `yandex_stt_grpc.py:_receive_loop` eou branch: `if self._last_partial_change_ts is None: return`. Не влияет на функциональность. Эффорт: 10 мин.
- [ ] **Post-pilot WAV review tooling** — batch workflow: transcript_dump.py `<UUID>` → вытаскивает из sofia_voice.db messages table по chat_id + форматирует в `[MM:SS] ROLE: text` файл. Для inbound calls (куда dial.py не запускался). Полезно когда пилот даст 30+ recordings. Эффорт: 30 мин.
- [ ] **`save_message()` timing instrumentation** — диагностика 9.3с blocking'а, наблюдавшегося в звонке `98332baf` turn 4 (17.04 вечер): TOTAL=10962мс, LLM=834мс — разница ~9293мс между `🎤 USER` логом и LLM `t0` snapshot'ом. Подозрение — SQLite WAL checkpoint / file lock на `save_message()`. **Одна точка данных, не воспроизводимо.** Перед слепым фиксом добавить per-call ms логирование в `save_message()` + `get_history()`: `logger.debug(f"sqlite save_message: {ms}ms")`. Дождаться повторения на 5+ звонках, тогда решать. Если регулярно >100мс → пересадить state на async SQLite (aiosqlite) или переразместить DB. Эффорт диагностика: 15 мин; фикс (если понадобится): 2-3 часа.
- [x] ~~**`yandex_stt_grpc.py` + `yandex_proto/` sync в DEV git-репо**~~ — закрыто 18.04 (коммит `296505c2`). Модуль в DEV, дальнейшие правки через DEV→scp. `yandex_proto/` был уже в git с 460b0a78 (только stt_grpc.py был missing).
- [ ] **Alice AI LLM для голоса** — попробовать Alice AI с max_tokens=80 и жёстким промптом "15 слов". В A/B 16.04 показала лучшее качество ответов, но многословна (user pause 2160ms). С ограничением длины может стать конкурентоспособной по скорости.
- [x] ~~**Добавить IP 72.56.64.91 в белый список Телфин**~~ — закрыто 08.04: Телфин не меняет GeoIP, двухсерверная архитектура — постоянное решение
- [x] ~~**fail2ban или UFW whitelist против SIP-сканеров**~~ — переведено в P0 (16.04), см. выше
- [ ] **UFW на VPS 185.207.66.201** — сейчас неактивен, SSH и Asterisk открыты в мир
- [ ] **Проверить STT pipeline voice_asterisk.py** — логи говорят "Whisper STT", CLAUDE.md говорит "Yandex SpeechKit". Привести документацию в соответствие с реальным кодом
- [x] ~~**Yandex STT gRPC v3 streaming**~~ — поднято в P1 16.04 (после voice research)
- [ ] **Per-turn JSON metrics baseline** — завести JSONL-лог `turn_metrics.jsonl` по одной строке на turn: `{call_uuid, turn_idx, stt_ms, llm_ms, tts_first_byte_ms, tts_total_ms, user_text_len, asst_text_len, barge_in_count, stage}`. Модуль `metrics.py`, инжект в `_process_turn`. Gain: p50/p95/p99 dashboard, обязательно ДО остальных latency-оптимизаций чтобы мерить эффект. Эффорт: 1 день. Идея #15.
- [ ] **Architecture A: Hybrid Brain** — async gpt-5.2+RAG «мозг» после каждого хода → stage_directive для kimi-k2. Качество текстовой Софии + скорость голосовой
- [ ] **Architecture B: Pre-recorded Bank** — 60 WAV-фраз покрывают 70% реплик, LLM-роутер выбирает код фразы. Gain: 350ms на 70% ходов
- [ ] **WebRTC кнопка в веб-виджете** — голосовой звонок в браузере
- [ ] **OpenAI Realtime API** — Voice V3, нативный voice-to-voice
- [ ] **journalctl --vacuum-time vs --vacuum-size** — на disk-rescue 15.04 использовали `--vacuum-size=200M`, что снесло логи sofia-voice целиком. На будущее предпочтительнее `--vacuum-time=Nd` для предсказуемого окна (хранить всё за последние N дней)
- [ ] **chore(format): run black on prompt files** — `sofia_prompt_v2.py` содержит pre-existing долг в `format_state_summary` (несжатые словари, trailing whitespace, mixed quotes), `black --check` не проходит и в DEV, и в PROD после split-deploy 15.04. Запустить black одним проходом, снять явное разрешение на `--no-verify` для будущих правок этого файла
- [ ] **chore(format): backport black on source_objects.py from dev to prod** — DEV имеет black-форматирование `detect_source()` и `load_object_context()` (multi-line re.sub, double quotes), PROD — старый single-line single-quoted формат. Ожидает отдельного коммита-портбэка в PROD, не блокер
- [ ] **Sofia в PROD web-канале не устанавливает source_object** — grep по web_api.py даёт 0 совпадений по `source_object`/`detect_source`. Значит даже после ATLANTIS_ADDON_INFRA_PORTBACK web-канал не будет активировать Atlantis-addon, пока не добавить вызов `detect_source()` + `state.source_object = 'atlantis'` при создании сессии с page_url содержащим `/atlantis` или при первом сообщении с ключевыми словами. Блокер для Sprint 3 «возврат виджета»
- [ ] **Analyzer классифицирует СВО как OBJECTION** — в DEV smoke 15.04 Analyzer отнёс СВО к stage=OBJECTION и затянул 10 RAG-примеров возражений, LLM пошёл по паттерну contrarg+next question вместо передачи менеджеру. Даже если Atlantis-addon попадёт в промпт (после ATLANTIS_ADDON_INFRA_PORTBACK) — новый триггер проиграет RAG-примерам в приоритете. Решение: либо hard-gate в pipeline по regex до Analyzer, либо перенести блок запретов из хвоста промпта в начало (CLAUDE.md «V6 прoмпт» пункт 1 — «жёсткие запреты в начале»)

## P3 — Техдолг / Стратегия

- [ ] **Удалить dead code `restore_assigned` + таблицу `lead_assigned`** — `core/bitrix.py:266-290 restore_assigned()` и `:577-622 save_original_assigned/get_original_assigned/_migrate_lead_assigned` + таблица `lead_assigned` (lead_id → original_assigned_by_id). После 19.04 stage-guard эта логика концептуально не нужна: мы больше не восстанавливаем оригинальный ASSIGNED в `finalize_lead`, а либо передаём лид раздатчику 24932 (happy-path, лид в NEW), либо молчим (skip-ветка, manager взял). `restore_assigned` не имеет call-sites. `save_original_assigned` вызывается при привязке к Тильда-лиду, но значение больше нигде не читается. Эффорт: 30 мин (удалить функции + `DROP TABLE lead_assigned` миграция в bot_server.py init или отдельным скриптом).
- [ ] **PROD origin audit — 10 локальных коммитов не запушены** — `git -C /opt/sofia-gpt log origin/main..HEAD --oneline` возвращает 10 коммитов (by-design, PROD никогда не пушится). После 19.04: `7e08e9a6`, `d60e9b0b`, `1146ea2c`, `3892c648`, `f105cb18`, `419ef20d`, `02ea4eab`, `49659ffb`, `ab086930`, `c032c0db`. Стоит провести audit-сверку: для каждого коммита подтвердить что в DEV есть эквивалент (SHA через format-patch совпадает) или явно отметить PROD-only (как `49659ffb` Observer был в прошлом). Результат в docs/PROD_DEV_SYNC_AUDIT_v2.md.
- [ ] **Extract chat_id derivation to shared module** — формула `str(9_500_000 + (int(md5(uuid)[:8], 16) % 1_000_000))` дублируется в `voice_asterisk.py:125` (`call_id_to_user_id`) и в `dial.py` (`audiosocket_uuid_to_chat_id`). Риск рассинхрона при смене формулы — transcript перестанет находить сообщения. Решение: вынести в `core/voice_ids.py` (одна функция, обе стороны импортируют). Эффорт: 15 мин. Сейчас защищено комментариями-напоминаниями в обоих местах.
- [ ] **dial.py concise parser — field index bug на Asterisk 20** — `find_new_telphin_uniqueid` читает `parts[12]` из `core show channels concise`, но на Asterisk 20 этот индекс не соответствует `uniqueid`. Наблюдалось на звонке `1776446150.338` (17.04 вечер): concise parser вернул non-None значение, но оно не совпало с реальным CDR uniqueid → `find_cdr_by_uniqueid` никогда не match'ил → dial.py висел до timeout. Сейчас компенсируется fallback matcher'ом timestamp+dcontext (коммит `fix(voice): dial.py always try fallback`). Правильный фикс: при следующем тестовом outbound параллельно запустить `asterisk -rx 'core show channels concise'` с live-каналом, посчитать поля и найти правильный индекс для uniqueid. После фикса сверить `captured_uniqueid` (в JSONL) с `uuid` (CDR uniqueid) — должны совпадать. Не блокер — fallback работает надёжно.
- [ ] **Turn-after-hangup race** — звонок 8857bb15 (16.04): `_process_turn` после `Connection closed` сгенерировал LLM response + Yandex TTS request впустую (клиент уже повесил). Растрата ресурсов, не критично. Fix: проверка `self._closed` между STT и LLM, early return.
- [ ] **RIZALTA промпт — тема на странный STT** — звонок 3f0c7ebd turn 3 (16.04): клиент сказал «Тут кстати примерные цены» с overlap на конец TTS → STT сбился → LLM выдал универсальный «Простите, плохо слышно, можете повторить?» из блока «Не расслышала». Проблема: один общий fallback теряет контекст. Варианты: (а) переформулировать блок на «Подскажите ещё раз, о чём спрашивали?» (звучит менее роботично), (б) хранить last_topic в state и использовать в fallback («По цене? По доходности?»). Мелочь в UX, но бросается в глаза.
- [ ] **Очистить legacy на VPS** — /root/dnstt, /root/microsocks, /root/go, /root/slowdns — от предыдущего владельца, безопасно удалить
- [ ] **AI-ассистент руководителя над Битрикс/amoCRM** — Telegram-бот, function calling (20-30 целевых функций: crm.deal.list, tasks.list, user.list), запросы натуральным языком ("у кого провал по сделкам", "кто не дорабатывает"). Потенциально отдельный продукт, переиспользует архитектуру Sofia. Начинать после стабилизации голосовой Sofia + RIZALTA
- [ ] **`sofia-bot-dev.service` — DEV-bot_server из `/opt/sofia-gpt-dev/`** — обнаружено 29.04 при RIZALTA_TEST_DEV_v1: bot_server.py из DEV не запущен как systemd-сервис (PROD bot_server обслуживает @humanAINeural_bot, ещё два `run_polling.py` — из `/opt/bot-dev/` другого проекта). Для регулярного DEV-тестирования RIZALTA-flow через @SofiaDev01_bot нужен отдельный systemd unit по образцу `sofia-radist-dev.service`. Эффорт: 30 мин (unit + smoke). P3.
- [ ] **Уровень 1 guard в `bot_server.py` для RIZALTA** — `_is_bitrix_session(user_id)` в `bot_server.py:163-166` возвращает True для rizalta-сессий (тогда как `sofia_radist_gateway._is_radist_bitrix_session` возвращает False для rizalta после `9947721d`). Если когда-нибудь RIZALTA придёт через @humanAINeural_bot — defense сработает только на уровне 2 (с громким `[BITRIX_RIZALTA_LEAK]` алармом и подменой на «Тестовый лид»). Не критично для текущего RIZALTA-канала (виджет → Radist). Эффорт: 15 мин (одна строка в `_is_bitrix_session` + 1-2 caller-блока). P3.
- [ ] **Tilda RIZALTA — сверить внешние материалы с investment-only positioning** — карточка `objects/rizalta_context.md` 29.04 жёстко зафиксировала: «исключительно инвестиционный продукт, не для личного проживания». Если на сайте rizalta или в маркетинговых материалах виджета остался старый текст вроде «можно приехать отдохнуть на 2 недели» — диссонанс между сайтом и Sofia собьёт клиентов. Сверить с Сергеем до боевого запуска RIZALTA-виджета. Не код, не наш scope, но parings-блок к карточке.

- [ ] **Логирование бота в stdout** — сейчас bot_server.py пишет в файл через print(), не видно в journalctl
- [ ] **Чистка мусора в prod** — лишние файлы
- [ ] **core/channel.py, core/observer.py** — рефакторинг каналов
- [ ] **BUG-004 EN-раскладка** — обработка латиницы в сообщениях
- [ ] **Обновить userMemories оркестратора** — устарели на сутки, упоминают V5 промпт, llama-4-scout, Flash v2.5 (давно kimi-k2 и eleven_v3), нет упоминаний ATLANTIS_ADDON_INFRA_PORTBACK, SPRINT_TEMPLATE_v2, виджета, safety-rules. Не блокер пока есть Gitpull, но создаёт противоречия в первый момент сессии. Делает оркестратор сам через memory_user_edits в начале следующей сессии, 10 минут.

## Завершено (28-29.04.2026)

- [x] **ATLANTIS_WIDGETS_DOC_v1** — коммит `027a353e`. `docs/ATLANTIS_WIDGETS_STATE.md` (15.0 KB, 14.9 KB после трим до лимита) — справочник по двум независимым путям привлечения на Атлантисе: форма «Получить презентацию» (Тильда → `t.me/m/QinKZEsTNmRi`, SOURCE_ID=397 в Bitrix через webhook) и HTML-виджет «Чат с менеджером» (`widgets/atlantis_chat_widget.html`, `t.me/m/Fiw3ldhkN2My`, дефолтный `SOURCE_ID=504`). Парная правка scp-строки в Gitpull-блоке CLAUDE.md.
- [x] **RIZALTA_CONTEXT_EXTRACT_v1 + REBUILD_v2** — коммиты `cf104e7a` + `e25bc8f7`. `objects/rizalta_context.md` (102 строки / 13.4 KB). Первая версия — извлечение фактов только из `VOICE_SYSTEM_PROMPT_RIZALTA` (`core/pipeline.py:646-780`). Вторая итерация — расширение из `rag_training_data.json` (4 диалога-сделки RIZALTA, 109 совпадений по ключевым словам) с детальной фактологией: схема гарантированного дохода, 10-шаговая дорожная карта сделки, налоговая оптимизация, реферальная программа. Структура зеркалит `objects/atlantis_context.md`.
- [x] **RIZALTA_REGISTER_v1 (DEV)** — коммит `9947721d`. Регистрация объекта `"rizalta"` в `config/source_objects.py` с узкими keywords (без `белокуриха` чтобы избежать false-match). Двухуровневая защита от Bitrix: уровень 1 в `_is_radist_bitrix_session` (short-circuit для rizalta-сессий с `[BITRIX_SKIP]` логом в 2 caller-блоках), уровень 2 (defense-in-depth) в `create_or_update_lead` — payload подменяется на `TITLE/NAME="Тестовый лид"`, `PHONE="89181011091"` через `title_override`-параметр в `create_lead`, дедупликация пропускается, громкий `🚨 [BITRIX_RIZALTA_LEAK]` ERROR-уровня. Observer-tag `f"{display_name} · rizalta"` через прокинутый `source_object` параметр в `notify_observer` + `get_or_create_topic`. Helper `_get_source_object(user_id)` в radist_gateway.
- [x] **RIZALTA_TEST_DEV_v1** — 4 автотеста PASS перед PROD-деплоем: `detect_source` 6 кейсов (включая защиту от false-match по «Белокуриха»), `_is_radist_bitrix_session` 4 кейса, level-2 fallback с моком `aiohttp.ClientSession` (проверено что `find_lead_by_phone` НЕ зовётся, payload подменён, лог `🚨 [BITRIX_RIZALTA_LEAK]`), Observer `get_or_create_topic` 3 кейса формата имени темы.
- [x] **RIZALTA_DEPLOY_PROD_v1** — коммит `3be32d68` PROD. Split-deploy: `core/bitrix.py` + `sofia_radist_gateway.py` через `git apply --index` (DEV ≡ PROD до коммита, чисто), `objects/rizalta_context.md` через `cp DEV → PROD`, `config/source_objects.py` ручная вставка только rizalta-блока через `Edit` (drift PROD без `prompt_addon`/black). Снапшоты `/tmp/*.prod.before_rizalta_deploy_20260429_095745`. Рестарт всех трёх сервисов (sofia-radist + sofia-web-api + sofia-gpt) — оркестратор настоял на полном покрытии level-2 во всех каналах. ERROR-grep за 2 минуты после рестарта — 0 строк. PROD detect_source-тесты прошли.
- [x] **RIZALTA_OBSERVER_TOGGLE_v1** — коммит `9f2d35bf` PROD. Env-флаг `RIZALTA_OBSERVER_ENABLED` (default `"false"`) в `sofia_radist_gateway.py` + `.env` (gitignored). Guard внутри `notify_observer`: для rizalta-сессий пишет `🚫 [OBSERVER_SKIP] source=rizalta reason=disabled_by_env phone=<phone>` и выходит. Atlantis и не-source-сессии без изменений. Включение обратно — 2 sed+restart команды.
- [x] **SERGEY_STATE_RESET_v1 + RADIST_HISTORY_SOURCE_RECON_v1** — без коммитов (data ops + read-only разведка). Чистка state Сергея (user_id `-40076372`, chat_id `38076372`, tg_username `sergey_7in`): `client_state` + `messages` + `radist_leads` (последний имел привязку к историческому Atlantis-лиду 265944 от 15.04). После второй чистки Sofia всё равно тянула старый контекст — read-only расследование показало что `sofia_radist_gateway.get_history()` читает из таблицы `radist_messages` (по chat_id, не user_id), которую первая one-liner-чистка пропустила. Урок: bot_server/web_api в `messages` (по user_id), Radist в `radist_messages` (по chat_id) — universal cleanup по user_id неполный.
- [x] **RIZALTA_CONTEXT_INVESTMENT_ONLY_v1** — коммит `4d79a8ae` DEV + `a5820989` PROD. После боевого smoke-теста Sofia предложила «для себя или для инвестиции» — структурный дефект позиционирования. Карточка поправлена: «исключительно инвестиционный продукт, не для личного проживания», новый раздел «Личное проживание собственника не предусмотрено», старый раздел «Спецусловия для собственников» (с пунктом «2 недели весной/осенью со скидкой 30%») удалён, 2 фактических пункта перенесены (приёмка → «Условия оплаты», перепродажа → «Юридическое»). Новый пункт первым в «Инструкциях для диалога»: «не предлагай RIZALTA для себя или для инвестиции». DEV ≡ PROD по md5 (`56a3303c06e1...`). Снапшот `/tmp/rizalta_context.md.prod.before_20260429_120015`.

## Завершено (21.04.2026)

- [x] **START_SESSION_PARITY** — закрыта парная правка к коммиту `af11212f` от 19.04. Сергей обновил локальный `~/projects_claude/start-session.sh` на Mac: добавлены 4 scp-строки и соответствующие `cat` в `collect_context()` для `docs/PILOT_WORKSTATION_RUNBOOK.md`, `docs/LESSONS_LEARNED.md`, `docs/VOICE_ARCHITECTURE.md`, `docs/VOICE_TECH_STACK_FULL.md`. Локальный скрипт теперь полностью зеркалит Gitpull блок CLAUDE.md (13 файлов). Claude.ai следующей сессии получит operator runbook, уроки, voice-архитектуру и полный voice tech stack.
- [x] **DIAL_PY_METRICS_v1** — коммиты `8ac8233f` + `f28cd748`. Per-turn EOU / LLM / TTS-first-chunk метрики aggregированные на триггере `⏱️ Turn timings:`, формат `[MM:SS] METRICS: eou=Nms llm=Nms tts=Nms` после каждой SOFIA-реплики. Timers `[MM:SS]` от call_start для USER/SOFIA/METRICS/AUTO_HANGUP; `[HH:MM:SS]` wall clock для Ringing/Answered/Ended. Post-call: `Recording:` путь + `Download (run from your Mac): scp ...` one-liner (gate ANSWERED+billsec≥5). `f28cd748` fix: recording path из `CDR.lastdata` (CALL_UUID RFC-4122), не `CDR.uniqueid` — два разных UUID в Asterisk.
- [x] **DIAL_LIVE_LISTEN_v1** — коммит `f501d719`. `bin/listen_live.sh` + `inotify-tools 3.22.6.0` на VPS. `inotifywait -e create` на `/var/lib/asterisk/recordings/YYYY-MM-DD/` + `tail -c +0 -F <wav>` + exit after 20с idle growth. Mac-side: `while true; do ssh sofia-voice 'listen_live.sh' | ffplay -nodisp -autoexit -f s16le -ar 8000 -ch_layout mono -; done`. SSH ключ обязателен (fix via `ssh-copy-id sofia-voice` на Mac).
- [x] **EXECSTOP_AUDIOSOCKET_CLEANUP_v1** — коммит `37e099a5`. `bin/hangup_audiosocket.sh` + `ExecStop=` в `sofia-voice.service`. Защита от orphan AudioSocket root-cause 20.04. Smoke: restart на активном канале → через 3с 0 orphan, `sofia-voice-hangup: hangup PJSIP/...` в syslog, MainPID 24230→42525 auto-restart. Part 2 (graceful hangup клиентом) — ExecStop не вызван, disposition=ANSWERED. Дисциплинарное правило «no restart mid-call» остаётся.
- [x] **PILOT_OPERATOR_RUNBOOK** — коммит `2304b075`. CLAUDE.md § «Рабочая станция пилота (three-terminal workflow)» с T1 listen_live (ffplay s16le 8k mono), T2 dial.py, T3 scp download.
- [x] **DIAL_PY_CHANNEL_GIVEUP_v1** — коммиты `38c42abf` (v1 flawed) + `75082b73` (v2 working). Root cause hang'a dial.py на invalid/unanswered номерах: Asterisk не создаёт CDR для не-ANSWERED outgoing (несмотря на дефолтную доку «outgoing always logged»), `wait_for_cdr` polling до 600с. Разведка: `cdr.conf unanswered=yes` не помогло бы — дефолт применяется, наш случай pre-channel failure. Fix v1 (`is not None` → channel_ever_seen) провалился: Asterisk на 1–2с создаёт channel в state `Ring`/`Other` даже при silent reject. Fix v2 требует 3 consecutive Up/Ringing polls (~3с sustained) перед `channel_ever_seen = True`. Smoke silent failure 88000000000 — **exit 35.4с** (было 600с); regression 79181011091 — ANSWERED 30с billsec ✓. Snapshots `/tmp/dial.py.before_channel_giveup_20260421_114322` (v1), `/tmp/dial.py.before_channel_giveup_v2_20260421_121732` (v2).
- [x] **TELFIN_AUTODIAL_RECON** — web-research 6 источников. Вердикт: НЕЯСНО из public docs, поднят как P2 с ожиданием ответа менеджера. Детали выше в P2.
- [x] **Pilot runbook doc** — новый `docs/PILOT_WORKSTATION_RUNBOOK.md` (workstation commands + состояние 21.04 + 6 особенностей). SESSION_LOG обновлён с детальной хронологией.

## Завершено (20.04.2026)

- [x] **VPS_UPGRADE_2VCPU** — sofia-voice 1→2 vCPU, 4 GB RAM (Timeweb Premium NVMe, ~1000₽/мес). nproc=2, load 0.00, RAM 3.2 GB free после boot. **Гипотеза не подтвердилась:** 1 vCPU не был виновником проглатываний. Настоящая причина — AudioSocket buffer overflow (см. ниже).
- [x] **VOICE_SPEAK_INSTRUMENTATION_v1** — добавлена per-chunk timing инструментация в `_speak` (~20 строк). Собрано 8600+ CHUNK-записей с 4 звонков (sleep_overrun / send_dur / inter_chunk_gap distribution). Снята в session-end коммите после локализации корневой проблемы. Snapshot `/tmp/voice_asterisk.py.before_instr_20260420_041752` на VPS.
- [x] **TTS_AB_MATRIX_v1_для_8ad55f5b** — 6 дополнительных curl-WAV (reply5/reply8 × baseline/48k/jane) плюс MixMonitor WAV звонка 8ad55f5b. Все скачаны в `/tmp/pilot_recordings_18_04/call_8ad55f5b_*`. 48kHz размер = 6× baseline (sanity check Yandex действительно возвращает 48kHz). Подтвердило: Yandex TTS не источник проглатывания ни на 8kHz, ни на 48k→soxr.
- [x] **CALL_8AD55F5B_CHUNK_ANALYSIS + INTER_CHUNK_GAP_ANALYSIS** — статистика 4373 чанков звонка 8ad55f5b. sleep_overrun median 947us p99 2147us, send_dur max 458us. Первый вывод (неверный): «Python чист, копать Asterisk». Пересмотрен после silent test показал детерминированные dropouts в статичном PCM-файле.
- [x] **docs/VOICE_TECH_STACK_FULL.md** — новый файл 49KB (719 строк). Полный технический стек voice pipeline от SIP INVITE до звука клиента: ASCII-диаграмма, 19 шагов, 25 параметров с `file:line`, таблица 27 файлов, 2 приложения (навигация по коду + diagnostic команды). Коммичен в составе `7f65fadd`.
- [x] **SILENT_TEST_DIAGNOSIS_b3cd88c8** — звонок 65с где Сергей молчал, услышал проглатывания **только в greeting**. Ключевая находка: greeting идёт через `_speak_pcm` из статичного PCM-файла (198318 bytes, md5 `8e553237010691bb13f92f900a11d1b0`). Детерминированный дефект, не связан с нагрузкой / STT / LLM. Побайтовое сравнение source vs MixMonitor: correlation **0.034** (шум), 209 loud→silent dropout-окон 10ms, 277 attenuation >5×. Путь виновника сузился до `_speak_pcm → AudioSocket → Asterisk core → MixMonitor`.
- [x] **VOICE_PACING_FIX_v1** — коммит `8f3eeaf0`. Корневая причина: `asyncio.sleep(0.018)` в обеих `_speak`/`_speak_pcm` функциях создавал wall_per_chunk ~19.1ms против 20ms audio rate → AudioSocket buffer overflow на Asterisk-side → drop'ы фреймов. Три итерации: (1) 18→20ms symmetric (greeting чист correlation 0.987, но в активном разговоре underrun 5% и stutter на каждом слове), (2) откат в сторону **асимметричного** фикса `_speak=0.019 / _speak_pcm=0.020` — учитывает разный event loop overhead (855us под параллельным gRPC+Silero vs 610us без нагрузки). Контрольный звонок `dff69ac4` (126с, 7 реплик, 3395 чанков): per-chunk wall 19 980us (цель 20 000), inter-chunk gap median **20 000us ровно**, 0 dropouts, 0 проглатываний в TTS. PROD не тронут (voice-компонентов в `/opt/sofia-gpt/` нет).
- [x] **VOICE_PACING_DOCS** — коммит `7f65fadd`. Документация обновлена: `VOICE_TECH_STACK_FULL.md` раздел 10 (pacing перемещён из «открыто» в «проверено+решено» с полными метриками), ASCII-диаграмма + шаг 14 + таблицы параметров разделов 5/6/7 обновлены. `VOICE_ARCHITECTURE.md` блок «Pacing fix (20.04)» с объяснением асимметрии.
- [x] **CHUNK_INSTRUMENTATION_CLEANUP** — снята временная инструментация из `_speak` (~20 строк, включая SLEEP_OVERRUN warning). Возврат к исходной структуре loop'ов + финальное pacing значение 0.019. Git diff только 3 изменённые строки `0.018 → 0.020/0.019/0.019`, 0 residuals. Snapshot `/tmp/voice_asterisk.py.before_cleanup_20260420_071727` на VPS.

## Завершено (19.04.2026)

- [x] **CLAUDE_MD_SPLIT_v1** — CLAUDE.md 57k→22k (−60 %) через split в два docs-файла. `docs/LESSONS_LEARNED.md` (36 прецедентных уроков 21.03–18.04 дословно с SHA/UUID/датами) и `docs/VOICE_ARCHITECTURE.md` (полная voice-архитектура — AudioSocket, Silero VAD v4, Yandex STT/TTS, Phase 1 VLAT-07, auto-hangup, MixMonitor, VOICE_PROMPT_MODE). В CLAUDE.md остались summary-блоки с явными ссылками «Полный текст: docs/FILENAME.md». Gitpull дополнен scp-командами на оба новых файла. Коммит `af11212f`.
- [x] **BITRIX_LEAD_RESCUE_RECON + WEBHOOK_NGINX_RECON** — инцидент 19.04, лид 266638 Elena +79124753743. Разведка: Sofia трижды финализировала лид через `set_lead_assigned(24932)+BP 152` несмотря на ручной перехват менеджером (Bitrix ASSIGNED=19622). Root cause: Bitrix webhook ONCRMLEADUPDATE не настроен — 0 hit на `/api/bitrix/webhook` за 14 дней, `manager_active` ни разу не установился. Endpoint полностью живой и доступен снаружи (HTTPS 200, Let's Encrypt, nginx `sofia-api.conf` proxy_pass `127.0.0.1:8080`, UFW 443 open). Мяч на стороне Bitrix. Решение через stage-guard (ниже).
- [x] **BITRIX_STAGE_GUARD_v1 (DEV + PROD)** — функция `get_lead_status(lead_id) -> str | None` в `core/bitrix.py`, fail-safe (None при любой ошибке API). Встроена в 3 точки: `finalize_lead` (единая для всех каналов), `radist_send_reminder` (2 свежих вызова — перед отправкой + перед finalize после sleep), `_radist_finalize_timeout`. Skip-ветка → `find_user_id_by_lead` → `set_manager_active(True)` → log `[FINALIZE_SKIP|REMINDER_SKIP|TIMEOUT_SKIP]` → return. `update_lead` безопасна, не проверяет. DEV `441dec63`, PROD `7e08e9a6` (format-patch clean apply, 3 сервиса перезапущены без ERROR). Smoke: `get_lead_status(266638)` → `"18"` ✓.
- [x] **PILOT_INVENTORY_19_04** — инвентаризация `/var/lib/asterisk/recordings/` + CDR + journalctl за 18-19.04. Факт: 9 WAV (9.98 MB) за 18.04, папки 2026-04-19 нет, последний файл 18.04 17:32 MSK. 13 inbound от 4 уникальных номеров. AUTO_HANGUP 14/14 detect→exec 0 cancel. EOU_WAIT median 1035мс ≈ baseline 1036мс (Phase 1 работает). 0 реальных ошибок, 6 HALLUC_TRUNCATED (штатно), 4 `[END]`. Сервисы active. После 18.04 17:32 MSK пилот-активности нет ~24ч.
- [x] **AUDIO_DROPOUT_8a7c4983** — разведка звонка `8a7c4983` (+79189007826, 124с). Yandex PCM полный (bytes=duration до сэмпла), `_speak` отправил целиком, 0 BARGE-IN/cancel/HALLUC. Артефакты «безопасном»/«управляющую» — на стыках длинных слов в середине реплик, не в начале. Гипотеза (E) coarticulation, не наша сторона. Побочное: `_speak` не логирует per-chunk (создан P1 пункт).
- [x] **AUDIO_DROPOUT_94ea75b1** — разведка звонка `94ea75b1` (+79160322905, 69.5с). Найдено отличие от 8a7c4983: 1 false-positive BARGE-IN на reply #2 через 497мс после старта TTS (Silero VAD threshold=0.5, 200мс cumulative на TTS-echo). Reply #2 сыграла 655мс из 10.79с, **10.1с аудио потеряно**, не возобновляется. Клиент реальный вопрос задал через 4.7с (не речь). Плотность artefact-кандидатов 0.46/с идентична 8a7c4983. Побочное: создан P1 пункт про Silero threshold tuning.
- [x] **TTS_AB_MATRIX_v1** — сгенерировано 12 WAV через прямой curl на Yandex TTS: 4 проблемных текста × 3 варианта (baseline alena 8kHz / alena 48kHz→8kHz soxr / premium jane 8kHz). Идентично `synthesize_tts_yandex` + `add_ssml_breaks`. Все 12 файлов `pcm_s16le/8000/mono`. Файлы в `/tmp/pilot_recordings_18_04/ab_*.wav`. **Вердикт Сергея после прослушивания: все три варианта звучат чисто, проглатывания нет ни в одном** → проблема **не в Yandex TTS**.
- [x] **MIXMONITOR_DISABLE_TEST** — эксперимент на живом звонке Сергея (+79310091644, ~1-2 мин). Snapshot `extensions.conf` → закомментированы 2 MixMonitor строки → `dialplan reload` (без restart) → звонок Сергея про Белокуриху/Атлантис → восстановление через `cp` из snapshot → diff пустой, md5 совпал. PID Asterisk 698 и sofia-voice 161279 не менялись. **Субъективный вердикт Сергея: «немного лучше, но не решено»** — MixMonitor часть проблемы, но не вся. Новый симптом зафиксирован: **щелчки в аудио Sofia** (jitter / CPU contention). Главный вывод дня: 1 vCPU VPS — основной подозреваемый, апгрейд поднят в P0 на 20.04.

## Завершено (18.04.2026)

- [x] **VLAT-07 Phase 1 — `max_pause_between_words_hint_ms=700`** — env-gated (коммит `393eb062`). EOU_WAIT median **1778→1036мс (−742мс, −42 %)**, p95 2148→1446мс. Targets оркестратора hit с margin. Рекомендация по Phase 2/3 (ExternalEouClassifier, semantic EOU) — не делать без метрик пилота (docs/VLAT_07_DEEP_DIVE + PHASE1_RESULTS).
- [x] **EOU instrumentation в yandex_stt_grpc.py** — per-turn `EOU_WAIT uuid=... turn=N eou_wait_ms=M text_len=K` (коммит `d2f875d1`). Baseline EOU_WAIT собран на 22 turns (docs/EOU_BASELINE_2026-04-18.md).
- [x] **`yandex_stt_grpc.py` sync в DEV git** — коммит `296505c2`. Устранён scp-only drift pattern (прецедент — тот же что с Observer `49659ffb` 08.04).
- [x] **RIZALTA prompt v4 → v4.1 → v4.2** — три итерации: v4 (`ffcde20e`) added ЧЕЛОВЕЧНОСТЬ / ФАКТЫ ДОСЛОВНО / МОЖНО ВАРЬИРОВАТЬ секции; v4.1 (`ab90940b`) replaced stress-problematic phrases («Дальше интересно?»/«Хочется подробнее?» Alena неправильно ударяла + duplicate «Продолжить?»); v4.2 (`110a6a3c`) natural phrasing («Про условия рассказать?»→«Рассказать про условия?», infinitive variant для offer).
- [x] **Auto-hangup после Sofia farewell (RIZALTA only)** — коммит `b78fe04c`. Hybrid keyword + [END] marker → AS_TYPE_HANGUP 0x00 frame после 1500 мс polite pause с barge-in interlock (cancel если user speaks). Gate `VOICE_PROMPT_MODE=rizalta` (Atlantis не затронут). L0 rollback через `AUTO_HANGUP_ENABLED=false` env.
- [x] **[END] handler fix** — коммит `19102904`. Pre-existing bug: `if "[END]" in answer_text` substring match ДО hallucination filter → LLM emit `[END]` в galluc tail, filter strips tail but `state.dialog_finished` уже flipped. Fix: swap order + anchor `answer_text.rstrip().endswith("[END]")`. Call `f184c7be` был первый проявленный regression; после fix — чисто.
- [x] **Call recording — MixMonitor + 72h→Opus rotation** — коммит `19a49fa6`. MixMonitor в обоих dialplan-контекстах. Cron 03:00/03:30 UTC. Inbound verified `aa568cc1` (792 KB, 49.5s, both sides audible).
- [x] **dial.py realtime transcript streaming** — коммит `4ce5881b`. subprocess(journalctl -f) + daemon thread + регекс matchers на USER/SOFIA/AUTO_HANGUP. Gate `STREAM_TRANSCRIPT=true` default. Time-filtered (start tail on state→Up, stop on Ended).

## Завершено (17.04.2026, вечер)

- [x] **Outbound dialer v1 — `dial.py` + `[outbound-audiosocket]` dialplan context** — `/opt/sofia-voice/dial.py` normalize phone → `channel originate` → CDR polling (uniqueid + timestamp+dcontext fallback) → JSONL в `/var/log/sofia-voice/outbound_YYYY-MM-DD.jsonl` → exit code по disposition. `[outbound-audiosocket]` zerkal [from-telphin] с `Set(CALL_UUID=${SHELL(/proc/sys/kernel/random/uuid)})` → `AudioSocket(${CALL_UUID},127.0.0.1:9090)`. UFW добавлено правило на второй Telphin IP `213.170.84.105:5090/udp`. Живой тест `98332baf` (119s, 7 turns RIZALTA) — нормальный UX. Коммит `96b68ae7`.
- [x] **dial.py fixes follow-up** — два related bug'а обнаружены после 96b68ae7: (1) `find_cdr_fallback` искал по `phone in channel` (для outbound CDR row не содержит номер) → заменено на timestamp+dcontext match; (2) `cdr_line_count` считал byte-level newlines, но CDR CSV с embedded newlines в quoted CallerID полях даёт 256 newlines vs 254 csv rows → замена на `sum(1 for _ in csv.reader(f))`; (3) `captured_uniqueid` добавлен в JSONL для post-mortem сравнения с CDR uniqueid (подтверждает P3 concise parser bug). Коммит `24dfc4b8`.
- [x] **Realtime status events + post-call transcript from SQLite** — dial.py теперь выводит `[HH:MM:SS] Originating → Ringing → Answered → Ended` через 1с polling `core show channels concise` (substring-match, не field-index). После summary — транскрипт из `/opt/sofia-voice/sofia_voice.db messages` table (chat_id derived via md5-formula mirror voice_asterisk.py:125) → файл `/var/log/sofia-voice/transcripts/transcript_<UUID>.txt` + stdout блок `--- Transcript ---` с формой `[MM:SS] SOFIA/USER: text`. JSONL дополнен `transcript_path` и `message_count`. Unit-тест на историческом `dfc4aee9` (7 сообщений) и `86c53f97` (1 сообщение). Коммит `ec410b3b`.
- [x] **YANDEX_STT_V3_INFRA_v1 (Phase 2A)** — grpcio 1.80.0 в venv VPS, `yandex_proto/` v3 скопирован (29 файлов), `/opt/sofia-voice/yandex_stt_grpc.py` создан как callback-based `YandexSTTStream`, smoke-tested с 320B silence (auth + bidirectional stream + clean close). Коммит `f2d8d8df`.
- [x] **YANDEX_STT_V3_INTEGRATION_v1 (Phase 2B+2C)** — Phase 2B: bifurcate через method-pointer `_process_audio` под `STT_MODE=grpc`, `text_override` в `_process_turn`, live-test `fe7eed5c` (длинные фразы с микропаузами склеены целиком). Phase 2C: continuous streaming + TTS-echo guard в `_on_grpc_*` через `_tts_playing and not _cancel_playback`, live-test `4b109925` (8 turns / 3 barge-in / 0 false turns / phone AEC работает). Коммит `e13aa3fa`.
- [x] **YANDEX_STT_V3_EOU_TUNING (Phase 2D)** — env-switch `YANDEX_STT_EOU_MODE=default|high` в `yandex_stt_grpc.py` + `voice_asterisk.py`. Live-test `4ad65942` подтвердил: EOU wait −320мс медиана / −620мс на коротких фразах, user pause −980мс на коротких, длинные фразы с микропаузами (100+ симв) НЕ порезаны. Prod `.env`: `STT_MODE=grpc, YANDEX_STT_EOU_MODE=high`. Rollback — одной env-строкой, без кода. Коммит `9114d825`.

## Завершено (17.04.2026, утро)

- [x] **FAIL2BAN_DEPLOY_v1** — установка fail2ban на VPS sofia-voice, два jail (sshd+asterisk), banaction=ufw, 9 SSH-сканеров забанены с первого старта. UFW + fail2ban = двухслойная защита. `ignoreip` включает 46.229.221.93 (Telfin), 72.56.64.91 (главный сервер), 77.106.252.172 (личный Сергея). `[asterisk]` — явный `backend=auto` на `messages.log` (journal у asterisk пустой). Детали и уроки — SESSION_LOG 17.04 (утро) + CLAUDE.md «Уроки из ошибок».

## Завершено (15.04.2026)

- [x] **DISK_RESCUE_v2** — VPS 185.207.66.201 диск 100% → 15%, корневая причина (сломанный с 2022 logrotate-конфиг) найдена и починена. Освобождено 25 GB. Asterisk + sofia-voice не пострадали, mtime configs не изменился. Бэкап старого конфига в `/etc/logrotate.d/asterisk.broken.20260415`
- [x] **CONTEXT_RECONCILIATION_v1** — сверка модели Claude Code с реальностью по 7 пунктам. Sprint 2 verified в production (лид 265712, @pordiregrwe → UF_CRM_TELEGRAM заполнен, БП 152 отработал)
- [x] **ORCHESTRATOR_RULES.md** — отдельный документ с правилами работы оркестратора, зафиксированы уроки 10-15.04
- [x] **logrotate для Asterisk** — починен корневой баг конфига (был в P1 как "настроить logrotate")
- [x] **UPDATE_GITPULL_DOCS** — блок «Gitpull» в CLAUDE.md дополнен 4-й scp-командой для `docs/ORCHESTRATOR_RULES.md` + `mkdir docs/`. Блок «Session End» обновлён. Локальный `start-session.sh` Сергей обновил синхронно. Коммит 5fee930c.
- [x] **ATLANTIS_CHAT_WIDGET_v1** — `widgets/atlantis_chat_widget.html` (15.9 KB), самодостаточный HTML-блок для Tilda T123. Десктоп: круг 72×72 с pulse-ring. Мобильный: плашка снизу с аватаркой. Ведёт в @SofiaOazis через вторую Telegram Business Link. Круг зелёный по фидбеку Сергея. Коммит 3de1fbf9.
- [x] **SPRINT_TEMPLATE_v2** — `docs/SPRINT_TEMPLATE_v2.md` (13.3 KB) на XML-структуре по гайду Anthropic. 9 обязательных секций + 4 опциональных + 2 встроенных блока (anti_overengineering, investigate_before_answering) + filling example. Коммит 057b6811.
- [x] **TEMPLATE_V2_WIRING** — шаблон проведён в ORCHESTRATOR_RULES.md (правило «новые контракты строить по SPRINT_TEMPLATE_v2.md») и в Gitpull блок CLAUDE.md. Коммит 71103849.
- [x] **SAFETY_RULES_v1 (DEV)** — блок «ЧЕГО SOFIA НЕ ДЕЛАЕТ» в `sofia_prompt_v2.py` (универсальные запреты: паспорт/реквизиты/коды/гарантии/доходность/бронь) и блок «ПЕРЕДАЁТ ДИАЛОГ МЕНЕДЖЕРУ» в Atlantis-addon (политика/юр-фин компании/СВО/дискриминация/незаконные схемы/мошенничество). DEV smoke #2 подтвердил работу общего ядра. Коммит 772cbe6e `--no-verify` (pre-existing долг в format_state_summary).
- [x] **SAFETY_RULES_DEPLOY_PROD (split, core only)** — портировано в PROD **только** `sofia_prompt_v2.py` (блок «ЧЕГО SOFIA НЕ ДЕЛАЕТ», работает во всех трёх PROD-каналах). Atlantis-addon часть отложена до ATLANTIS_ADDON_INFRA_PORTBACK (в PROD отсутствует механизм чтения prompt_addon целиком). PROD smoke с СМС-триггером подтвердил работу. Все три сервиса active, 0 ошибок за 5-мин мониторинг. PROD коммит d60e9b0b, без push.
- [x] **GAPS_CLOSE_BEFORE_NEXT_SESSION** — закрыты 3 пробела перед следующей сессией: (1) стилистические правила Сергея («lf»=да, запрет ритуальных оборотов, короткое признание ошибок, один шаг=одно сообщение, «человеческим языком») добавлены пунктами 6-10 в ORCHESTRATOR_RULES.md «Стиль ответов»; (2) 4 критичных memory-файла добавлены в Gitpull блок CLAUDE.md (project_prod_dev_drift, user_sergey, project_voice_sprint_next, project_voice_vps); (3) в P3 заведён пункт на обновление userMemories оркестратора. Коммит 3081ea0e.

## Завершено (11.04.2026)

- [x] **Деплой Radist username→Bitrix в PROD** — sofia_radist_gateway.py + core/bitrix.py скопированы в PROD, sofia-radist.service перезапущен, логи чистые. PROD коммит 1146ea2c. (11.04)

## Завершено (10.04.2026)

- [x] **Аудит Атлантиса** — docs/AUDIT_ATLANTIS_2026-04-10.md, 8 секций, зафиксирован переход на @SofiaOazis, 5 открытых проблем с приоритетами
- [x] **Спринт 1: merge bot_server.py** — Observer portback из PROD в DEV (6 блоков, 86 строк), единственное различие осталось — env-default OBSERVER_CHAT_ID. DEV стал deploy-ready. Коммит 75b779a5.
- [x] **Спринт 2: Radist username → Bitrix** — chat.username из webhook → UF_CRM_TELEGRAM + COMMENTS. DEV коммит 8b504869, PROD деплой 11.04 коммит 1146ea2c.
- [x] **Фаза 4: systemd + health check** — sofia-voice.service на VPS, Restart=on-failure, auto-restart протестирован. Голосовой канал защищён от ребутов.
- [x] **TTS A/B тест: v3 locked** — live A/B: v3 > MLv2 > Flash v2.5. Eleven v3 = production TTS (10.04)
- [x] **Stress Research** — ruaccent 75%/388ms отклонён, U+0301 не документирован → ручной словарь (10.04)
- [x] **ElevenLabs v3 Research** — model_id, streaming, TTFB, PVC compatibility, pricing (10.04)
- [x] **Sync DEV от VPS** — voice_asterisk.py + core/pipeline.py синхронизированы, коммиты 8d468477 + 04d9a710 (10.04)

## Завершено (09.04.2026)

- [x] **Radist dedup fix** — save_message+notify_observer вынесены из цикла queue, одна запись на пачку сообщений (DEV+PROD)
- [x] **Voice Research Sprint** — 54 аудиосемпла, 9 LLM, 16 TTS, 7 STT, отчёт RESEARCH_REPORT_VOICE_AGENT_v1.md
- [x] **Voice Stack Tuning v2** — kimi-k2 LLM, Flash v2.5 → v3 TTS, новые VoiceSettings, VAD 300ms, SSML breaks
- [x] **RIZALTA Prompt v2** — канонический питч, «всегда вопрос», Софья→София, 6 примеров диалогов
- [x] **Groq платный план** — решено через kimi-k2 (throttling не наблюдается)

## Завершено (28.03–06.04.2026)

- [x] **Фикс SOFIA_PATH в prod .env** — prod бот писал в dev БД, повтор бага 21.03 (05.04)
- [x] **Фикс логирования bitrix.py** — logging.basicConfig в bot_server.py, логи [BITRIX] видны в journalctl (05.04)
- [x] **Диагностика лидов 264270/264264/264260/264252** — все распределены, клиенты не заходили в бот (05.04)
- [x] **Деплой Bitrix-интеграции в PROD** — finalize_lead, start_distribution_bp, дедупликация по телефону (04.04)
- [x] **Фикс ASSIGNED_BY_ID=24932 перед БП** — БП 152 требует конкретного ответственного (04.04)
- [x] **Фикс manager_active в state_manager** — краш таймаута "no such column" (04.04)
- [x] **Новый webhook-токен с bizproc** — uviwr0b350u3m6gf (04.04)
- [x] **Фикс redirect Тильды** — был dev-бот, заменён на prod (04.04)
- [x] **E2E тест БП раздачи** — форма → лид → TG → 15 мин → BP 152 → раздача (04.04)
- [x] **docs/LEAD_LIFECYCLE.md** — архитектура CRM-интеграции (04.04)
- [x] **docs/NEW_CLIENT_ONBOARDING.md** — инструкция подключения нового ЖК (04.04)
- [x] **Голосовой стек Телфин → Asterisk → AudioSocket → STT/LLM/TTS** — полный pipeline на VPS (06.04)
- [x] **Asterisk + SIP trunk Телфин** — PJSIP, порт 5090, Registered (06.04)
- [x] **AudioSocket сервер** — voice_asterisk.py, TCP протокол, двусторонний аудио (06.04)
- [x] **Nginx LLM proxy** — 72.56.64.91:8095 для Groq/OpenAI/ElevenLabs из России (06.04)
- [x] **TTS streaming ElevenLabs** — first audio ~400-500ms вместо 5-8 сек (06.04)
- [x] **VAD 0.7s + barge-in** — перебивание останавливает TTS, контекст сохраняется (06.04)
- [x] **Фикс промпта "Повторите" на "Завтра"** — добавлен пример для дат/времени (06.04)
- [x] **A1: VAD threshold 0.4s** — снижено с 0.7s, -300ms ощущаемой паузы (07.04)
- [x] **ElevenLabs voice tuning** — stability=0.35, similarity=0.79, speed=1.19 (07.04)
- [x] **Research Qwen3-TTS** — 30 аудиофайлов, WaveSpeed API, отчёт готов (07.04)
- [x] **TASK_DUMP_SOFIA_PROMPTS.md** — полный дамп всех 9 промптов системы (07.04)
- [x] **Bitrix в Radist gateway** — radist_leads, ATL-привязка, таймауты 15/60/15, manager_active (08.04)
- [x] **Окно матчинга 60 сек** — find_recent_atlantis_lead фильтр >=DATE_CREATE (08.04)
- [x] **RIZALTA промпт v1** — VOICE_SYSTEM_PROMPT_RIZALTA + VOICE_PROMPT_MODE switch (08.04)
- [x] **Git recovery после краша** — DEV 3 коммита, PROD 5 коммитов по каналам (08.04)
- [x] **ElevenLabs multilingual_v2** — switch для PVC голоса Nastya (08.04)
- [x] Промпт V5-V5.2: persona, few-shot, позитивные инструкции (b34e5ffc → 8e0a7738)
- [x] Barge-in pending: замена debounce, склейка фраз (f83631ae)
- [x] meeting_agreed context check (e9b47719)
- [x] Voice pipeline v4: Groq llama-4-scout primary (TTFT 99ms)
- [x] Битрикс: БП раздачи (template 152) — finalize_lead() (c1987ff5)
- [x] Дедупликация транскриптов Retell (3-уровневая)
- [x] Budget парсинг (дефис, "от-до", числительные)
- [x] Исследование LLM: Groq, YandexGPT, Fireworks, Together, DeepSeek
